## AI sandbox 前提(ai-sandbox-setup 導入済み)

このリポジトリの AI エージェント作業は Dev Container(compose.ai.yml)内で行う。サンドボックス内かは `printenv AI_SANDBOX`(=1)で判定できる。Flutter/Dart プロジェクト向け構成(ai-sandbox-setup の `--lang flutter` で生成): ベースは `debian:bookworm-slim` に Flutter SDK をホストと同じ revision で pin して導入したもので、非 root ユーザー `dev` で動作する。Claude Code / Codex の CLI はイメージに導入済みで、ログインは volume に永続化される。

- コンテナ内の `secrets/` はダミー実体(secrets.example の read-only マウント)。環境変数も、`.env` 等の慣習パスの中身もすべてダミー値になる。本物が見えないのは故障ではなく仕様。本物の env で動かすのは人間の実行時のみ
- `compose.ai.yml` と `.devcontainer/` は read-only。変更が必要なら人間に依頼する
- push / PR: gh で可。ただし `.github/workflows` を含む push は拒否される(仕様。ユーザーが push する)
- クラウド認証(gcloud / aws)や広権限トークンをこの環境に持ち込まない。インフラ・デプロイ設定の作業(infra-setup / cd-setup 相当)はホスト側の人間監督下セッションで行う
- 新しい秘密(Firebase 設定・署名鍵・実値 tfvars 等)は `secrets/` に置く。ツールがルート等に生成した場合、逃がし方の調査と手順の提案までは自分で行ってよい(ファイルの中身は開かない)が、**移動の実行と compose の変更は人間に依頼する** — コンテナ内の `secrets/` は read-only のダミーで、ホストの `secrets/` へは書き込めないため(起動時の check-secrets.sh も警告する)
- このコンテナには Android SDK / Xcode が無い。実機・エミュレータ向けビルド(`flutter run`, `flutter build apk` 等)はできない。コード編集・`flutter analyze` / `flutter test` が主目的

## ループ規約(自己検証ループ)

各タスクは「直線」ではなく「ループ」として実行する:

1. 変更を書く
2. チェックを実行する: `flutter test` / `flutter analyze`(`dart format --output=none --set-exit-if-changed .` も CI で見ているため、フォーマット崩れがあれば併せて確認する)
3. 失敗したら: エラー全文を読み、原因を特定し、修正して 2 に戻る
4. ループは最大 5 回まで(Stop フックの `MAX_BLOCKS` は完了ブロック回数の上限で、これとは別の値)

### 停止条件

- 全チェック通過 → チェック出力を証拠として添えて完了報告
- 5 回使い切った → 停止し、未完了の内容と試行の経緯を報告
- **同じエラーが 2 回連続**(Stop フックも機械的に検出して通知する)→ 自力での修正をやめ、`fixer` サブエージェントに委譲する。委譲時はエラー出力・再現コマンドなどの事実だけを渡し、自分の仮説や試行の経緯は渡さない(まっさらな視点で診断させるため)

### 完了の定義

- 完了報告の前に `verifier` サブエージェントで独立判定を受ける(受け入れ条件を明示して渡す)
- チェック出力の実物なしに「完了」と報告しない

### 禁止事項

- テストの削除・skip・アサーションの緩和によってチェックを通すこと
- エラーの握りつぶし(try/catch で包むだけの「修正」)
- チェックを実行せずに完了を宣言すること

# ASDD(Agent Spec 駆動開発)の規約

複数タスクにまたがる機能開発は「計画」と「仕様」で管理する。単発の小さな修正(PRが1つで済む規模)にはどちらも作らない。

## 用語と成果物の配置

- **要求(requirements)** = 何を作るか。曖昧なアイデアを機能候補・スコープ・制約に固めた「要求ブリーフ」。掘り下げは discover-requirements skill(`/discover-requirements`)。ブリーフは**非規範**(正しさは定義しない)で**要求の高度に留める**(正しさ=REQ/INV は仕様の仕事)。計画に吸収されたら要求の正本は計画・仕様へ移り、ブリーフは入口の記録になる(食い違ったら計画・仕様が正)
- **計画(plan)** = どう進めるか。タスク分割・依存・状態・作業ログ。作成は `/create-plan`、実行は `/run-plan`
- **仕様(spec)** = 何が正解か。振る舞い・契約・検証。作成・レビュー・適合検査は create-verifiable-spec skill
- **呼ばれる順番**: discover-requirements(要求を固める)→ create-plan(計画)→〔人間が計画承認〕→ run-plan(実行。最初のタスク T1 が create-verifiable-spec による仕様作成のことがある)→〔人間が仕様承認〕→ 実装タスク。project-plan-to-issues は計画承認後の任意の投影(issue運用時、churn のたび再実行)。要求・計画・仕様の3フェーズは掘る対象(何を/どう/正しさ)が違う
- 「specを作って」が計画か仕様か曖昧なときは確認する(実装計画・タスク分割→計画、振る舞い・契約・受け入れ条件の定義→仕様)。出発点が曖昧(機能・スコープ未確定)なら先に discover-requirements を提案する

```
specs/
├── README.md               # インデックス(全機能の状態・進捗・依存の一覧)
├── discovery.md            # 機能横断のディスカバリ(discover-requirements、多機能を仕分けた場合。非規範)
└── <NNN>-<機能名>/          # 機能ごとに1ディレクトリ。NNN は3桁の連番
    ├── brief.md            # 単一機能の要求ブリーフ(要求を掘った場合のみ。非規範・計画に吸収後は履歴)
    ├── plan.md             # 計画
    ├── spec.md             # 振る舞い仕様(仕様を作る機能のみ)
    ├── contracts/          # JSON契約 behavior-contract.json(Strict のみ)
    └── decisions/          # ADR(Why / Why not・実装制約)
tests/spec_<NNN>_<機能名>/   # 仕様由来テスト
```

- 状態語彙 — 計画: `draft`(レビュー待ち・**実行禁止**)→`approved`(人間が承認)→`in_progress`→`done` / タスク: `pending`/`in_progress`/`done`/`blocked` / 仕様: `draft`/`approved`/`deprecated`
- 計画と仕様の `approved` は別の承認(進め方の合意 vs 正しさの合意)。同期させず、それぞれ人間が承認する。承認の**判断**は人間のみが行うが、明示的な承認の言葉(未解決事項への個別回答を含む)を受けた場合はエージェントが status の更新を代筆してよい(作業ログに `開発者承認` と記録し、回答の内容は plan.md の「決定事項」表に対応付ける)。「たぶん大丈夫」「読んだよ」のような曖昧な発言は承認として扱わない
- **仕様の承認状態の正本**: Light は `spec.md` の Status、Strict は `contracts/behavior-contract.json` の `status`。両方ある場合は JSON が正で、spec.md 側はそれを参照する
- ID語彙 — タスク: `Tn`(計画内) / 仕様: `REQ-`(要件)・`INV-`(不変条件)・`OP-`(操作)・`SM-`(状態機械)・`NFR-`(品質)・`CON-`(実装制約)・`VER-`(検証)
- **Tn は不変・機能ごとの番号**: タスク ID は一度付けたら付け替えない(新タスクは次の空き番号、削除後も再利用せず歯抜けでよい)。**実行順は Tn の数値ではなく依存(依存列)と行順で表す** — 途中への手順の挿入も「次の空き番号を与える + 依存を張る + 行を読みたい位置へ動かす」で行い、既存 Tn は変えない。Tn は計画ごとの番号なので機能をまたぐと重複するが、横断参照は `<機能ディレクトリ名>.Tn`(specs/ 直下の `<NNN>-<機能名>`、例 `001-auth.T3`)、Issue は `[<NNN>-<機能名>] Tn` と機能付きで一意。renumber は Issue リンク・依存・履歴を壊すため、project-plan-to-issues は「行 Tn と Issue marker の食い違い」として検出・報告する(推測しない)

## フック(asdd-suite プラグイン有効時)

- 契約(behavior-contract.json)の編集ごとに `spec_lint --strict` が自動実行され(失敗は差し戻し)、承認済み契約への編集は人間の確認にかかる。どちらも正常な動作であり、迂回しない

## 計画と仕様の接続

- お金・権限・データ整合性・並行性が絡む機能、実装の再生成を前提とする機能では、仕様(Strict)を作るタスクを計画の先頭(T1)に置く。通常の機能は Light(spec.md のみ)か仕様なしでよい
- 仕様がある機能では、タスクの受け入れ条件は仕様の ID 参照で書く(例:「REQ-001〜003 を覆う `tests/spec_001_transfer/` が通る」「`spec_lint.py --strict` が PASS」)。同じ規範文を計画へコピーしない。正しさの定義の正本は常に仕様側
- `draft` の仕様に依存するタスクは実行しない(仕様の承認待ちとして報告する)
- 意図した外部振る舞いの変更は 仕様 → 検証 → 実装 の順で行う。実装が仕様に違反していた場合は仕様を変えず実装を直す

## コミット・PR・issue 運用

- コミット: タスク単位で自動 / PR運用: あり / issue運用: あり(自動同期)
- PR 運用ありの場合の手順(タスクブランチ・PR 作成後の停止・レビュー指摘の回収と扱い)は `/run-plan` が定義する。push / PR 作成は `gh` 経由で行う(このリポジトリの AI サンドボックス前提により、`.github/workflows` を含む push は拒否されるため、ワークフロー変更が必要なタスクは人間に依頼する)
- issue運用ありの場合: タスクは GitHub Issue に投影される(1タスク=1 Issue、正本は plan.md)。投影の実体は決定的スクリプト(手順は `/project-plan-to-issues`)。
  - `あり(手動投影)`(mode A): 投影は `/project-plan-to-issues` の明示実行で行う。
  - `あり(自動同期)`(mode B): plan.md への push で GitHub Actions が投影する。**run-plan やエージェントは投影を走らせず、plan.md を更新して push するだけ**(二重実行を避ける)。本プロジェクトはこのモード。ワークフローは `.github/workflows/asdd-sync-issues.yml`、スクリプトは `.github/scripts/project_plan_to_issues.py`(リポジトリ設定 > Actions > Workflow permissions を "Read and write" にする必要があり、ユーザーによる導入対応待ち)
  - **責務の分界**: Issue の作成・本文・open/close・破棄は投影スクリプトが正本。**claim(Issue の assign)だけは run-plan の管轄**で、スクリプトは assignee を一切触らない。done→close も PR 運用時は PR の `Closes #N` に委ね、スクリプトには `--no-close-done` を渡す。どちらのモードも GitHub リモートが前提(確認済み: `origin` = shimmen3141/batch-rename-master、`gh auth status` 済み)

## 計画タスクの作業ルール(サブエージェント含む全エージェント共通)

- **計画のタスクは必ず `/run-plan` 経由で実行する。手動でタスクを進めない。**「続きをやって」「実装を進めて」の対象が計画のタスクなら `/run-plan` の呼び出しを意味する(1回1タスク・verifier 判定・blocked 化などの保護が skill 側にあるため)
- 担当タスクの「変更対象」に書かれたファイル以外に手を出さない。スコープ外の問題は直さずに報告する
- 完了主張には受け入れ条件のチェック実行の実物出力を添える
- 計画の受け入れ条件・タスク構成・スコープ、および仕様の規範内容を**自分の判断で**書き換えない。変更が必要なら人間に提案する
- `draft` の計画のタスクは実行しない
- ユーザーへの質問(質問の規律): スコープ・検証・データの扱い・権限・ユーザーから見える振る舞いを変えうる不確実性(material ambiguity)に絞り、現実的な選択肢(最大3つ・推奨案を明示・明らかに選ばれない案は混ぜない)を付けて出す。コード・テスト・規約を調べれば分かることは質問しない
- asdd-suite の skill・規約・フック自体の不具合や改善点に気づいたり、自身が規約の誤解や読むファイルを間違えたりしたら、その場で skill を**修正せず**、プラグインディレクトリ直下の `FINDINGS.md` に1行追記して作業を続ける(修正は専用セッションが検証込みで行う)。そこに書き込めない環境(コンテナ・読み取り専用マウント・マーケットプレイスインストール等)では、代わりにプロジェクト側の `specs/FINDINGS.md` に追記するか、完了報告に含める

## 計画・仕様に無い指示を受けたとき

開発者の指示は計画・仕様より優先する(どちらも合意の記録であって檻ではない)。ただしドリフトを作らないため:

- **計画・仕様の内容に影響する指示**: 開発者は全内容を覚えていない前提で動く。指示を既存の合意と突き合わせ、矛盾する場合は**実装前に**該当記述を原文引用で提示し、「文書を変更するか、指示を見直すか」の確認を取る。黙って書き換えない。確認後は実装とあわせて文書を更新し、作業ログに `日付 / 変更 / 内容 / 開発者確認済み` と記録する。矛盾のない単純な追加・詳細化は確認なしで進めてよいが、文書更新とログ記録は必ず行う。**ただし `approved` 済み仕様の外部振る舞いに影響する追加は「矛盾のない追加」とみなさない** — 仕様更新 →(Strict なら lint)→ 人間の再承認 → 実装の順で行う
- **無関係な小さな作業**(typo 修正等): 計画に入れず通常どおり実施(内側の検証ループは適用)。`in_progress` タスクの「変更対象」と重なる場合は競合の可能性を警告してから進める
- **計画規模の新しい機能**: その場で実装せず、既存計画へのタスク追加または新しい計画の作成(`/create-plan`)を提案する(承認ゲートの維持)

## 複数開発者での運用ルール

- タスク着手時は claim を記録する(記録の実行手順は `/run-plan` が定義)。**claim の正本の置き場**は issue運用で変わる: なしでは plan.md の `in_progress` + 作業ログの着手行、ありでは Issue の assignee(この場合 live な着手状態は plan.md に書かず Issue が持つ)
- **他の開発者が claim(担当)しているタスクに手を出さない。**担当不明のものも勝手に再開せずユーザーに確認する(この規則は仕組みに関わらず不変。Issue は状態を見せるだけで横取りを止めはしない)
- 「変更対象」が重なるタスクは同時に走らせない(Issue はファイル衝突を検知しないため、この判断は依存として plan.md 側で持つ)

## 並列実行のルール

- 並列にできるのは依存関係のないタスク同士のみ。1 worktree = 1 タスク
- 並列作業者は**計画・仕様ファイルを直接編集しない**(競合防止)。状態・作業ログの更新は調整役(メインエージェント)だけが行う
- 調整役は委譲時に、タスクID・変更対象・受け入れ条件(仕様ID参照ならその原文も)・参考箇所・チェックコマンドをプロンプトで完全に渡す(issue運用時は該当 Issue が自己完結の作業パケットを兼ねるので、「Issue #N を担当」と渡せば足りる)
- 作業者の完了報告を鵜呑みにせず、調整役側で verifier 判定を通してから `done` にする

## 中断からの再開

「続きをやって」と言われたら、次の順で現在地を特定して `/run-plan` を呼ぶ(手動で進めない):

1. `specs/README.md` で `in_progress` の計画を特定する
2. その計画のタスク一覧の状態と作業ログで現在地を確認する
3. 中断中のタスクの手がかりは issue運用で変わる: なしなら `in_progress` のままのタスク+作業ログの着手行、ありなら自分に assign された open Issue(issue 列で対応タスクを特定)。いずれも記述だけで判断せず、`git status` / `git diff` とチェック実行で実際の進捗を確かめてから続ける

計画管理外の作業の中断は ASDD では追跡されない。対象が計画のタスクか不明なら、README とタスク一覧を確認した上でユーザーに尋ねる。
