# T09 読み込み導線の再設計の実装(種類選択 → 選択 UI → リスト画面)

## 目的

- 読み込み導線の再設計の実装(種類選択 → 選択 UI → リスト画面)

## 入力と依存

- 依存: T07, T08
- 同じworkspaceの`spec.md`、contract、decisions、仕様由来テスト。

## 変更範囲

- 旧planで定めた成果範囲。詳細は凍結履歴を参照する。

## 受け入れ証拠

- T7 の仕様が **approved**、かつ **T8 の結果が出ている**(未了なら着手せずブロック報告)。
  - 種類選択 → 選択 UI → リスト画面 の流れが動く。**画像経路は枠のみ**で、選ぶと「写真機能で対応予定」を
        示す(未実装であることが利用者に分かる)。
  - 蓄積が取り消され、選択のたびにリストが置き換わる(T7 の REQ どおり)。
  - 「作成日時: 不明」の強調が**作成日時ソート時のみ**になる(002 REQ-013)。
  - T7 で決めた並び替えの扱い(連番が無いときのドラッグ)が実装されている。
  - 該当 REQ/VER を覆うテストが通り、既存テストが緑のまま(退行なし)。`flutter analyze`/`dart format` PASS。色は `AppColors`。
  - 実機での導線確認はホスト側(手順は emulator-verification.md を更新する)。
- 参考: T7、T8、`docs/design/Bulk Renamer.html`、既存の `FileSourceBar`/`FileListView`

## 作業記録

旧ASDD 0.xから、実際に記録されていたcheckpointだけを移行した。将来予定やlive状態には使わない。

- 2026-08-05 / verifier の非ブロッキング指摘2件を修正: (1) ⚠️ **Android では `sourceLocation` が常に null** で、REQ-009(場所の副題)と REQ-012(跨ぎ警告)が**本番で発火しない**穴があった(fake にだけ location を入れていたためテストで気づけなかった)。SAF の document URI から親フォルダ名を導出する `locationOfDocumentUri` を追加し、T8 で得た**実物の URI 形**を使うテストを足した。(2) `ui_entry_test.dart` に旧導線の名前と、蓄積/置き換えのどちらでも通る識別力ゼロのテストが残っていたので、跨ぎ警告の観点へ統合し名称を整理。
  - 2026-08-05 / T9 着手 / shimmen3141。Issue #83 を assign(claim)、ブランチ asdd/004-file-source/T9(承認ブランチから分岐)。
  - 2026-08-05 / T9 完了 / **新導線を実装**: `FileKind`(画像/動画/文書/すべて)+ 種類選択シート → 選択 UI → リスト置き換え。**画像・動画は枠のみ**でソースを呼ばず「写真機能で対応予定」を通知(REQ-011)。**文書**は MIME フィルタ、**すべて**はフィルタ無し。`addFiles` → **`setFiles`**(置き換え・同一ハンドル集約・全件選択。REQ-004/005)。ポートから **`pickFolder` を削除**し `pickFiles({mimeTypes})` に(REQ-001)。**親フォルダ跨ぎの警告**(REQ-012)。002 は **`manualOrderMatters`**(連番の有無)でドラッグハンドルと「カスタム順」チップを出し分け(REQ-014)、**「作成日時: 不明」の強調は作成日時ソート時のみ**(REQ-013)。テスト +18(合計 275)。**275 tests PASS / analyze 0 issue / format PASS**。verifier PASS(試行1回。5種のミューテーション試験で検出力も確認された)。
