# T08 実機スパイク: SAF ドキュメント URI でリネームできるかの確認(ホスト検証)

## 目的

- 実機スパイク: SAF ドキュメント URI でリネームできるかの確認(ホスト検証)

## 入力と依存

- 依存: T04
- 同じworkspaceの`spec.md`、contract、decisions、仕様由来テスト。

## 変更範囲

- 旧planで定めた成果範囲。詳細は凍結履歴を参照する。

## 受け入れ証拠

- 実機/エミュレータで、`OPEN_DOCUMENT` で選んだ1ファイルに対し `saf_util` の `rename` を呼び、
        **成功するか失敗するか**を確認した(ホスト側作業。手順は `docs/development/emulator-verification.md` に追記)。
  - 併せて、**フォルダのツリー権限で得た URI** でも同じ確認を行い、両者の可否を比較した。
  - 結果と、失敗時の代替案(ツリー権限へ戻す / MediaStore の書き込み同意)を作業ログに記録した。
- 参考: ADR-001(プラグイン選定。`saf_util` を選んだ決め手が rename の有無だった)、005 の計画

## 作業記録

旧ASDD 0.xから、実際に記録されていたcheckpointだけを移行した。将来予定やlive状態には使わない。

- 2026-08-05 / **OQ-1 は先に解消**(開発者の実機確認): システムのファイル選択画面の**右上のケバブメニューに「Select all」があり、フォルダ内を一括選択できた**。したがって導線 B のままで一括選択は成立し、ツリー権限へ戻す案 (a)・アプリ内選択画面を作る案 (b) は採らない。※この解消は 004 spec 側の記述なので **PR #80(T7)に反映**する。
  - 2026-08-05 / PR #84 作成(Closes #82)。verifier PASS(試行1回)。補足指摘(ツリー側の生 URI)も追記済み。マージ待ちで停止。
  - 2026-08-05 / T8 着手 / shimmen3141。Issue #82 を assign(claim)、ブランチ asdd/004-file-source/T8(T8 は T4 依存なので dev から分岐。T7 の仕様変更は PR #80 側)。使い捨ての確認画面 `lib/spike_rename_check.dart` を追加し、`main.dart` の AppBar に入口を置いた(**確認後に削除する**)。①`OPEN_DOCUMENT` のドキュメント URI で `saf_util.rename` を試す、②ツリー権限で得た URI で同じことを試す、の2ボタン。手順を emulator-verification.md に追記。
  - 2026-08-05 / T8 完了 / **実機スパイクの結果: ①ドキュメント URI・②ツリー権限とも改名に成功**。導線 B(`OPEN_DOCUMENT` のドキュメント URI を書き戻し先にする)の前提が成立し、**設計変更は不要**。実測で判明した005 向けの制約2点を **ADR-001 と discovery の 005 項へ記録**した: (a) ⚠️ **改名すると URI が変わる**(`.../IMG_0010.jpg` → `.../IMG_0010_t8.jpg`)ため `sourceHandle` は stale になり、005 は改名のたびにハンドルを更新する必要がある。「元に戻す」は**新しい URI に対して元の名前で改名し直す**形になる。(b) ⚠️ `saf_util.rename` の戻り値の `name` は**ドキュメント URI 経由では空**だった(ツリー権限経由では正しく返る)ため、新しい名前は要求値を使うか取り直す。使い捨てのスパイク画面(`lib/spike_rename_check.dart`・`main.dart` の入口・手順書の節)は**削除済み**。257 tests PASS / analyze 0 issue。
