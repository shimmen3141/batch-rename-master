# asdd-suite FINDINGS

asdd-suite の skill・規約・フック・生成物への気づき(不具合・不備・改善点)を1行ずつ記録する。修正は専用セッションが検証込みで行う。コンテナ/読み取り専用マウントのためプラグイン直下の FINDINGS.md に書けないので、プロジェクト側のここに追記している。

- 2026-07-26 / 規約 / 仕様由来テストの配置が `tests/spec_<NNN>_<機能名>/` 固定だが、Flutter/Dart は `flutter test` が `test/` 配下を走査するため `test/spec_<NNN>_...` でないと拾えない。言語ごとのテストディレクトリ差を規約が扱っておらず、計画の決定事項で読み替えて対応した。create-verifiable-spec / 規約に「言語慣習に合わせて test ルートを読み替える」注記があると手戻りが減る。
- 2026-07-26 / フック(asdd-sync-issues.yml)/ mode B の投影トリガが `branches: [main]` 固定。PR運用で統合ブランチ(dev)に PR をマージする運用だと、dev マージでは Issue の投影・クローズが走らず main 到達までズレる。ユーザーが後から `dev` をトリガに追加して解消。PR運用×非main統合ブランチの相互作用を asdd-setup / project-plan-to-issues が確認・案内できるとよい。
- 2026-07-26 / フック(asdd-sync-issues.yml)/ issue運用「あり」かつ PR運用「あり」なのに、生成された workflow の投影コマンドに `--no-close-done` が付いていない(コメントで言及のみ)。CLAUDE.md 規約は PR運用併用時に `--no-close-done` を付けろと指示しており、生成物と規約が不一致。本プロジェクトでは dev マージでのクローズが望ましかったため実害は無かったが、PR運用時に done→close が二重管理になりうる。
- 2026-07-27 / 計画(create-plan)/ 仕様の VER-005(determinism_test: REQ-013/INV-004/CON-001)が、計画のタスク分割(T2〜T6=VER-001〜004)のどのタスクの受け入れ条件にも割り当てられておらず、全タスク done 後の「全体の受け入れ条件」最終検証で初めて未作成が発覚した。create-plan がタスク分割時に「契約の全 VER がいずれかのタスクに被覆されているか」を照合できると、末端での取りこぼしを防げる。今回は完了検証時に determinism_test を追加して解消。
- 2026-07-27 / 手戻り / ブランチの分岐元(main か dev か)と Claude 帰属(コミットの Co-Authored-By・PR 本文のフッタ)の方針が計画・規約に無く、既定で main 分岐・帰属付きで進めた後にユーザー指摘で dev 分岐・帰属なしへ修正(コミット amend / force-push)が発生。asdd-setup か create-plan で「統合ブランチ」「コミット/PR の帰属方針」を初期に確認できると手戻りを防げる。
- 2026-08-02 / スコープ被覆漏れ / PRD §4.2 の「リアルタイム警告(重複・桁不足のプレビュー時ハイライト)」という振る舞いが、discover-requirements の機能仕分けでどの機能候補(002〜006)にも明示的に割り当てられず、002 のウィジェット実装(T4)で参考デザインに warn 表示があって初めて「002 spec に無い」と気づいた。今回は開発者判断で 005 に寄せて解消。discover-requirements か create-verifiable-spec が「PRD の各振る舞い(特に横断UIの警告・トースト類)がどの機能の REQ に落ちているか」を突き合わせるチェックがあると、実装段階での気づきを前倒しできる。
- 2026-08-02 / 手戻り(人間要望)/ 参考デザイン(claude design)が 001 実装完了・002 仕様承認の後に到着し、(a)approved 済み 001 の振る舞い追加(元名トークンの大小変換)、(b)discovery 除外項目に触れる undo、(c)PRD §4.2 の未割当警告、(d)ウィジェット層の視覚方針(セマンティックカラー)——と、承認済み成果物や確定スコープに関わる論点が複数まとめて発生した。個別対応(仕分け・discovery追記・将来送り)で吸収したが、ASDD で「デザイン成果物の取り込み時期」が定義されておらず、要求/仕様フェーズより後にデザインが来ると仕分けコストが実装フェーズに寄る。discover-requirements の入力にデザイン/プロトタイプがある場合の扱い(要求確定前に取り込むか、後入れ時の再仕分け手順)を規約化できるとよい。
- 2026-08-02 / 手戻り(ASDD フロー) / 仕様作成タスク(T1)の PR を、spec.md が `draft` のまま(人間が Status を approved にする前に)dev マージしてしまい、承認を別 PR で後追い反映する手戻りが発生した。「T1 PR のマージ」と「spec の承認(Status 変更)」は別物だが、PR をマージすると承認したつもりになりやすい。run-plan / create-verifiable-spec が「spec が draft の T1 PR はマージ前に Status=approved を確認/促す」ガード(PR チェックやレビューテンプレの注意書き)を持てると、この取り違えを防げる。今回は別 PR(#44)で Status=draft→approved と open_questions 確定を反映して解消。
- 2026-08-04 / 波及のタスク化漏れ / 承認済み仕様(001/002)への波及を 004 spec の「波及」節に洗い出して再承認まで済ませたが、そのうち「002 REQ-002: 時系列ソートを createdAt→modifiedAt」だけが**どのタスクの「変更対象」にも入らないまま**残り、T2 の verifier 申し送りで初めて表面化した(`lib/ui/file_list/file_sort.dart` が未変更)。仕様側の波及リストと計画側のタスク/変更対象が突き合わされないと、「承認したのに誰も実装しない要件」が生まれる。create-verifiable-spec が波及を洗い出したら、run-plan / create-plan 側で「波及項目 → 担当タスクの変更対象」への反映(またはタスク追加提案)を促すチェックがあるとよい。
- 2026-08-04 / 仕様更新と検証の非同期 / 001 契約に INV-005 を追加した際、既存 VER-005 の `covers` に INV-005 を足して lint も PASS したが、**実体のテスト(determinism_test.dart)には対応するアサーションが無い**まま approved になった。`spec_lint --strict` は covers の ID 整合は見るがテスト実体の有無・内容までは見ないため、「宣言だけされて検証されていない不変条件」が通ってしまう。承認済み仕様に ID を追加する更新では、VER 側テストの追記を同じタスクの受け入れ条件に含める運用(または lint の警告)が要る。今回は T2 のレビューパスで検出し、同 PR でテストを追加して解消。
- 2026-08-04 / 規約と verifier の期待の食い違い / CLAUDE.md の「複数開発者での運用ルール」は issue運用ありのとき claim の正本を Issue の assignee とし「live な着手状態は plan.md に書かない」と定めているが、verifier は完了判定時に「plan.md の作業ログに T2 の着手行(claim)が無い」を申し送り事項として挙げた。規約どおりでも verifier からは記録漏れに見えるため、issue運用時に作業ログへ着手行を書くのか書かないのかを、規約・run-plan・verifier のプロンプトで揃えるとよい(本プロジェクトでは可読性のため着手行も書いている)。
- 2026-08-04 / サンドボックス(ai-sandbox-setup 側)/ read-only マウント(`.devcontainer/`・`secrets/`)が git の checkout/merge をブロックする。`.devcontainer/Dockerfile` をホスト側で更新した後、コンテナ内で対象ブランチへ切り替えようとすると「unable to unlink old ...: Read-only file system」で失敗し、さらに**失敗した merge が未コミットの編集を巻き戻す**事故が起きた(仕様の approved 化編集をやり直し)。`secrets/.gitkeep` の削除状態も同様に checkout を止める。回避は `git update-index --skip-worktree secrets/.gitkeep` と、内容が同一の read-only ファイルを index に反映してから ff-merge する手順。ai-sandbox-setup 側で「read-only マウント対象は skip-worktree にしておく」初期設定と、この既知の失敗パターンのトラブルシュートがあるとよい。
- 2026-08-04 / サンドボックス(ai-sandbox-setup 側)/ `~/.gitconfig` が永続化されていない(volume は `/home/dev/.claude` と `/home/dev/.codex` のみ)ため、Rebuild のたびに `git config --global --add safe.directory /workspace` が消える。Windows ホストの bind mount は uid=0/gid=0 で見えるので、これが消えると git が「dubious ownership」で全面停止する。ホスト側の `devcontainer.json` の `postCreateCommand` に safe.directory 設定を入れて恒久解消(ワイルドカードではなくパス明示)。加えて、**ホスト側で `flutter run` 等を実行すると生成物(`ios/Flutter/flutter_export_environment.sh`・`macos/Flutter/ephemeral/...`)が root 所有 755 で作られ、コンテナ内の `dev` から書けず `flutter test` が失敗する**(gitignore 済みなので削除して再生成が対処)。テンプレート/トラブルシュートに両方あるとよい。
