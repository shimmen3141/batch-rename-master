# 手動確認: 008:T07 行と警告の情報階層

## 何を確かめてほしいか

自動testで閉じられなかったものだけを残してあります。**5項目、20〜30分**です。

| # | 確かめること | なぜ機械で閉じられないか |
|---|---|---|
| 1 | 画像のpreviewが行に出る。件数の多いfolderでも待たされず、アプリが重くならない | 速度とメモリはmachine検証できない(残余risk **N-5**)。**emulatorで構いません** |
| 2 | **動画**のpreviewが行に出る。**縦横比が保たれている** | Kotlinの`MediaMetadataRetriever`はCIで動かせない(**N-5**)。`getScaledFrameAtTime`が比を保つかはAndroid側でしか分からない(**N-6**) |
| 3 | 狭幅で `作成日時: 不明` が最後まで読める | `004:T10`で見切れを観測した条件そのもの。widget testは通っているが、実機のfontで再確認する |
| 4 | **文字サイズを最大**にしても、どの行が「不明」か分かる | 端末のfont設定を機械側で再現していない(**N-7**) |
| 5 | 警告帯を開いたとき、内訳が以前より多く読める | 「読めるか」は人の目で見る。件数の閾値を機械で決めない |

**preview が出ない行があること自体は不具合ではありません。** 種別アイコンに落ちるのが設計上の
正常な結果です。**壊れた絵や、行の高さのガタつき**があれば不具合です。

## 対象

| | |
|---|---|
| branch | `asdd/008-ui-alignment/T07-row-and-warning-presentation` |
| code の最終commit | `0e66b24` |
| PR | #159 |

**これより新しいcommitは記録(`specs/`)とtestだけ**で、appの動きは変わりません。

**branchの切り替えは不要です。** Agentがこのbranchのまま待機しており、結果を受け取るまで
動かしません。

## 事前準備

起動手順は[`docs/development/emulator-verification.md`](../../../../docs/development/emulator-verification.md)に従います。

**このtaskは`MainActivity.kt`(Kotlin)を変更しています。ホットリロード(`r` / `R`)では反映
されません。** 動いているappがあれば`q`で終了し、`flutter run`を実行し直してください。

```powershell
git branch --show-current
git log --oneline -6
flutter run -d <device_id>
```

**期待**: branch名が `asdd/008-ui-alignment/T07-row-and-warning-presentation` で、一覧に
`0e66b24 fix(asdd-008/T07): 画像bufferの持ち主を境界で確定させる` が含まれる。appが起動する。

依存は増えていないので`flutter pub get`は不要ですが、実行しても害はありません。

### emulatorで構いません

**実端末は要りません。** ここで見たいのは Android framework の API が返すもの
(`MediaMetadataRetriever` の frame、`getScaledFrameAtTime` の縦横比)で、emulator も同じ
framework を通ります。

ひとつだけ**参考値として受け取る**ものがあります。手順1の「scrollが引っかからないか」は、
emulator では PC の性能に引きずられます。**実端末が手元にあれば手順1だけ追試する価値が
ありますが、無ければ emulator の結果で先へ進めます**(残余risk **N-5** として記録します)。

### fixtureを置きます

**`.worktrees/t07-fixtures/` に用意してあります。**格子と正円を描いた画像24枚(縦横比は8種類)と、
`broken-fixture.jpg`(中身が画像でない`.jpg`)、`notes.txt`、`report.pdf` です。

> **格子と円なのは、歪みを見えるようにするためです。** 正方形の枠へ切り取って表示するので、
> 風景写真では縦横比が崩れていても気づけません。**セルが長方形に見える / 円が楕円に見えたら
> 歪んでいます。**

repositoryのルートで、**folderごと送ります**。

```powershell
adb push .worktrees\t07-fixtures /sdcard/DCIM/
adb shell ls /sdcard/DCIM/t07-fixtures | Measure-Object -Line
```

**期待**: `Lines : 27` と表示される。

`adb push` は宛先のfolderを作るので、`DCIM` が無い emulator でもそのまま通ります。

> **置き場所が `.worktrees/` なのは、host から見える場所がそこだからです。** `build/` と
> `.dart_tool/` と `.pub-cache/` は**container専用のvolume**で、host のrepositoryには
> 現れません(`compose.ai.yml`)。`.worktrees/` は bind mount の中にあり、gitの管理外です。

確認が終わったら、host 側の `.worktrees/t07-fixtures/` ごと消して構いません。

### 動画だけは用意できません

このcontainerに動画のencoderが無いため、**動画のfixtureはそちらでご用意ください。**
どれか一つで足ります。

- **emulatorのカメラアプリで5秒ほど録画する**(いちばん短い経路です。emulatorには疑似カメラが
  あり、録画すると `DCIM/Camera` に入ります)。
- PCにある`.mp4`を送る:
  ```powershell
  adb push "C:\Users\<あなた>\Videos\sample.mp4" /sdcard/DCIM/Camera/
  ```
- emulatorのウィンドウへ**ファイルをドラッグ&ドロップ**する(`/sdcard/Download` に入ります。
  その場合は手順2でショートカット「Download」を開いてください)。

**横長の動画にしてください。** 正方形や縦長だと、縦横比が崩れていても気づきにくくなります。

### 権限

初回は「すべてのファイルへのアクセス」の許可を求められます。許可してください
(`013`の権限フロー)。許可しないと file browser が開きません。

## 手順1 — 画像のpreviewと、件数の多いfolder

1. 「**ファイルを選ぶ**」を押す。
2. 種類のシートで「**すべて**」を選ぶ(Androidでは「画像」「動画」はまだ写真機能待ちで、
   選ぶと案内のsnackbarが出ます)。
3. app内のfile browserが開く。まず**保存場所の一覧**が出るので、それを押す。
   次に出るショートカットの「**DCIM**」を押し、`t07-fixtures`へ入る。

   > **保存場所の名前は端末が付けたものをそのまま出しています**(004の決定D-2)。emulatorでは
   > `SDCARD` のように大文字になることがありますが、**そのまま進めて問題ありません。**
   > 複数並んだ場合は、押した先に `DCIM` のショートカットが出る方が目的の場所です。
4. **20件以上をcheckして**「**確定**」を押す(fixtureが27件あるので、全部でも構いません)。

**見るところ**

- [ ] 各行の左に**写真の縮小画像**が出る。
- [ ] preview が出るまでの間、行が**アイコンのまま**で、高さや文字の位置が**動かない**
      (絵が入った瞬間に行がガタつかない)。
- [ ] **一覧を上下に速くscrollする。** 引っかかり・数秒の固まりが無い。
- [ ] `broken-fixture.jpg` の行が、**画像アイコンでも写真でもない別のアイコン**(壊れた画像の
      印)になっている。

> ここで見ているのが残余risk **N-5** の前半です。**「重い」と感じたら、その体感をそのまま
> 教えてください。** 秒数を測る必要はありません。

## 手順2 — 動画のpreview(**N-5 / N-6**)

1. 手順1と同じ経路で、今度は**動画を含むfolder**(`DCIM/Camera` または `Movies`)から
   **動画を3件以上**選んで確定する。
2. 動画が無ければ、端末のカメラで5秒ほど録画してから同じ操作をしてください。

**見るところ**

- [ ] 動画の行に**その動画の一場面**が出る(黒一色ではない)。
- [ ] **縦横比が崩れていない。** 横長の動画が縦に潰れて見えたり、人や物が細長く/平たく
      なっていない。**枠は正方形で、はみ出す分は切り取られるのが正しい**見え方です。
- [ ] 動画のpreview が出ない場合も、**壊れた絵ではなくフィルムのアイコン**に落ちている。

> ここが **N-6** です。**「歪んでいるかもしれない」と迷ったら、その行のスクリーンショットを
> ください。** 正方形へ切り取っているので歪みが見えにくく、迷うのが普通です。

## 手順3 — 狭幅で `作成日時: 不明` が読めるか

**端末を縦向きにしてください。**(`004:T10`で見切れを観測したのはこの向きです。)

1. 一覧の上のソートで「**作成日時順**」を選ぶ。

**見るところ**

- [ ] 各行の下に「**作成日時: 不明**」が出て、**末尾の「不明」まで欠けずに読める**。
      Androidでは作成日時を取得できないため、**全行が「不明」になるのが正しい**動作です。
- [ ] 「不明」の左に**警告の印**が付き、色が他の文字と違う。
- [ ] 「場所」(`Camera` など)が**日時とは別の行**に出ている。
- [ ] 幅が足りないときに**削られるのは「更新日時」の側**である(「作成日時」が優先して残る)。

## 手順4 — 文字サイズ最大(**N-7**)

1. 端末の 設定 > ディスプレイ > **フォントサイズ**(または「表示サイズとテキスト」)を
   **最大**にする。
2. appへ戻る(再起動は不要です)。

**見るところ**

- [ ] どの行が「不明」なのかが**警告の印で分かる**。
- [ ] 行が**赤い縞模様(overflow)**になっていない。
- [ ] 文字が枠の外へはみ出していない。

> **「作成日時: 不明」の文字が途中で `…` になるのは、この設定では想定内**です(印は消えない
> ので、どの行かは分かります)。**印まで消えている / 行が崩れている**場合だけ不具合です。
>
> 一覧の**上のヘッダ**が崩れることがありますが、**それはこのtaskの範囲外**で `008:T10` が
> 引き受けます(残余risk **N-8**)。見つけたら教えてください。直すのは別taskです。

確認したら、フォントサイズを**元に戻して**ください。

## 手順5 — 警告帯

1. 一覧の下の「**変更する名前を設定する**」を押す。
2. 「**＋ 自由テキスト**」を押し、`photo` と入力して確定する。**トークンはこれ1つだけ**に
   します(全fileが同じ名前になり、重複警告が件数ぶん出ます)。
3. シートを閉じ、一覧の上に出る「**警告 N 件(…)**」を押して開く。

**見るところ**

- [ ] 内訳が**scrollできる**。
- [ ] **一度に3件以上**読める(以前は2件しか入りませんでした)。
- [ ] 警告の領域が**画面の半分を超えて**一覧を覆っていない。

> **ここは「以前よりまし」までしか直していません。** 警告を各fileの行へ出す作り直しは
> `T15`/`T16`が引き受けます。**読みにくいと感じたらそのまま教えてください** — その感想は
> `T15`の入力になります。

## 後片付け

```powershell
adb shell rm -r /sdcard/DCIM/t07-fixtures
```

emulatorを捨てる予定なら、これも不要です。PC側の`.worktrees/t07-fixtures/`も消して構いません。

改名を実行していなければ、端末のfileは変わっていません。**手順5でルールを作っただけでは
改名されません**(実行buttonを押していなければそのままです)。

## Windows desktop について

**このtaskでは必須ではありません。** 触れる場合の期待値だけ書いておきます。

- 画像のpreview は**出ます**(decodeがDart側なので両platform共通)。
- **動画のpreviewは出ません。** 種別アイコン(フィルム)に落ちるのが正しい動作です
  (`task.md`の「Windows desktopの動画thumbnailは実装しない」)。
- Windowsでは作成日時が取れるので、手順3の「不明」は**出ません**。

## 結果の返し方

**自由形式で構いません。** 番号と、見えたとおりの言葉で教えてください。スクリーンショットが
あれば助かります(手順2の縦横比と、手順4の文字サイズ最大は特に)。

Agentが結果を`task.md`へ要約し、証拠のcommitと日付を記録します。**返信用のtemplateは
用意していません。**
