# T24 folderで行を束ねる案を定義する

## 目的

複数folderのファイルが一覧に混ざるときの見せ方を、**行ごとの副題**から
**folder行で束ねる形**へ変えるかを決める。決めるところまでがこのtaskで、実装は別taskへ分ける。

## 受領した要望(2026-09-18、原文)

> 要望1: 複数フォルダにまたがる場合、各行の詳細にフォルダ名を書いても見づらいので、フォルダの行を追加する案を思いつきました。以下のようにフォルダ行(名前だけの行なので縦幅は小さくてよい)で各ファイルをまとめ、各ファイルはフォルダ行に対して少しインデントさせるイメージです。また、フォルダ行をクリックすることで、配下のファイルを非表示にできたりするとよいです(フォルダ行のアイコンのフォルダが開いたものになったり閉じたものになったりする。ファイルがフォルダ行にしまわれるようなアニメーションがあるとユーザもイメージしやすい)。
>
> - フォルダA
>   - ファイルA-1
>   - ファイルA-2
> - フォルダB
>   - ファイルB-1
>   - ファイルB-2

観測の出所は [`T08`](../T08-load-affordance-and-path/task.md) の
「manual確認の結果(2026-09-18。2回目)」。

## 入力と依存

- 002 REQ-010 と `specs/002-file-list/spec.md` の「場所のサブ情報」
  (**行が場所を出すのは混在時だけ**。2026-09-18 に `T22` で開発者が再承認した)。
  代表例 7b・7c が**行の副題**を前提に書かれているので、**この案を採るなら代表例も書き換わる**。
- 002 の並び替え(REQ-002/014)。**folderで束ねると「一覧全体の並び」と両立しない** —
  日時順・サイズ順・カスタム順が folder の境界を越えられなくなる。ここが最大の論点である。
- 005 の実行対象と 002 の選択(`〇/〇件を選択`)。folder行に checkbox を持たせるか、
  折りたたんだ中の選択をどう数えるか。
- `T09`(表示モード)と `T18`(行の結果表示)。行の構造を触る先行taskである。

## 先に確かめるべき前提(未解決)

**いまこの案が効く経路が製品に無い。** Android は 004 REQ-016 で**1 folder内の選択に限られ**、
Windows desktop の picker も folder を跨げない
(`development-findings/2026-09-18-desktop-picker-cannot-span-folders-so-the-multi-folder-path-is-unreachable.md`)。
したがって、

1. **複数folderの混在を製品経路から到達可能にするか**(004 REQ-016 / REQ-012 の見直し)
2. それを**しない**なら、この案は将来の保険として仕様だけ決めるのか、**やらない**のか

は開発者の判断である。**1が「しない」で確定するなら、このtaskは「やらない」で閉じてよい。**

## このtaskの成果物

- 002 の行構造に対する決定(採否、採るなら代表例 7b・7c の書き換え案と並び替えとの両立の仕方)。
- 採る場合の実装taskの切り出し(折りたたみ、アニメーション、選択の数え方)。

## Current state / handoff

- Last checkpoint: 要望を受領してtask化した
- Blocker category: decision
- Waiting for: 上の「先に確かめるべき前提」1の判断(複数folderへ到達させるか)
- Requested action: 複数folderの混在を製品経路から到達可能にするか(004 REQ-016/REQ-012の見直し)を決める。「しない」ならこのtaskは「やらない」で閉じる
- Evidence revision: 未着手(観測は `T08` の2回目の実機確認)
- Next Agent action: 判断を受けて、採否と 002 の改訂案を書く
