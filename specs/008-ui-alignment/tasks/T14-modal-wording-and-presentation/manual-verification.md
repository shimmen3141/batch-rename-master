# 手動確認: modalの文言と見せ方

**この手順書は`T14`の実装後に completed させる。** 現時点では観測する対象と、
使い回す手順だけを置く。

## 使う端末と準備

**`013:T07`の手順書をそのまま使う** —
[`specs/013-safe-android-rename/tasks/T07-implement-android-file-browser/manual-verification.md`](../../../013-safe-android-rename/tasks/T07-implement-android-file-browser/manual-verification.md)
の `## 0.`(対象buildの見分け)と `## 準備するファイル`(PowerShellでfixtureを置く)を
先に実行する。**ここへ書き写さない。**

**結果の提示を確かめるには件数が要る。** 実装後に具体化するが、**10件以上**を一度に
改名する fixture を用意する(現在の結果表示は高さで打ち切られる)。

## 観測する対象(実装後に手順へ具体化する)

- 確認のダイアログ: **何を聞かれているか**と、**実行すると何が起きるか**が読めること。
- **「ルール設定」のsheet**(狭幅ではルールの編集が必ずここを通る): 開いたときの高さ、
  scroll、閉じ方。**token追加ボタン列とルールの列が同時に読めるか。**
- 結果の提示: **件数が多いときに「旧 → 新」が読める**こと。
- 狭幅の端末で、いずれも切れずに読めること。

**読み込む種類の選択(画像 / 動画 / すべて)はここで見ない** — `T08`が導線ごと確定させる。

## 報告

結果は会話で自由に書いてよい。決まった書式は不要である。
