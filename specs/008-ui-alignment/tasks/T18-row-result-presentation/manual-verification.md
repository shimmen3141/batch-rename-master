# 手動確認: 008:T18 行の結果表示

## 何を確かめてほしいか

自動testで閉じられなかったものだけを残してあります。**4項目、10〜15分**です。

| # | 確かめること | なぜ機械で閉じられないか |
|---|---|---|
| 1 | 変更後名の**緑と赤が実機で見分けられる** | 色の値はtestで固定できるが、実機の画面で区別できるかは見ないと分からない |
| 2 | 行の警告が**現在名の上に右寄せ**で出て、実機のフォントで読める | widget testはtest fontを使うので日本語の実寸が違う |
| 3 | **行の警告が押しやすい**(`008:T16`から引き受けた残余risk) | tap範囲の実サイズは実機でしか分からない。`T16`はemulatorでしか見ていない |
| 4 | ルール未設定と元名だけのルールで **`（変更なし）`** が出て、強調されていない | 実機の配色で「弱い色」に見えるかを見る |

**この確認で見たいのは「見分けられるか」「読めるか」「押せるか」です。** どの行が
警告対象かという判定は001が持っていて自動testで固定済みなので、内容の正しさを
突き合わせる必要はありません。

## 対象

| | |
|---|---|
| branch | `asdd/008-ui-alignment/T18-row-result-presentation` |
| code の最終commit | `e5aceed` |
| PR | #164 |

**これより新しいcommitは記録(`specs/`)とtestだけ**で、appの動きは変わりません。

**branchの切り替えは不要です。** Agentがこのbranchのまま待機し、結果を受け取るまで動かしません。

## 事前準備

起動手順は[`docs/development/emulator-verification.md`](../../../../docs/development/emulator-verification.md)に従います。

**このtaskはDartだけを変更しています。** 動いているappがあれば`R`(ホットリスタート)で足ります。

### 実端末でお願いします

**手順3(tap範囲)は実機でしか見られません。** `008:T16` のmanual確認をemulatorで
行ったため、tapの確かめが弱いまま残っており、**このtaskが引き受けています**
(2026-09-02の開発者の決定)。手順1・2・4はemulatorでも成立します。

### fixtureは`T07`のものを流用します

`.worktrees/t07-fixtures/` の画像24枚をそのまま使います。まだ端末に入っていれば送り直す
必要はありません。入っていなければrepositoryのルートで:

```powershell
adb push .worktrees/t07-fixtures /sdcard/DCIM/
```

## 手順

各手順の先頭でルールを作り直してください。**前の手順のルールが残っていると後半が成立しません。**

### 手順1 — 緑と赤が見分けられる

1. `DCIM/t07-fixtures` を読み込み、**全選択**にします。
2. ルールのトークンを**「固定文字」1つだけ**にします。値は `photo`。シートを閉じます。

- **確認A**: `.png` の24行の変更後名(`photo.png`)が**赤**、`.jpg` / `.txt` / `.pdf` の
  3行(`photo.jpg` など)が**緑**になっている。
- **確認B**: 赤と緑が**一目で見分けられる**。目を凝らさないと分からない、ということが無い。

> **なぜこうなるか**: 拡張子を保つので重複するのは`photo.png`の24枚だけです。他の3件が
> 正常なのは正しい動きです。

### 手順2 — 警告が現在名の上に右寄せで出る

手順1の状態のままで見ます。

- **確認A**: `.png` の行で、**現在名(`IMG_xxxx.png`)より上**に `名前が重複` が出ている。
- **確認B**: その文字が**行の右端側へ寄っている**。
- **確認C**: 文字が途中で切れて種別が読めない行が無い。
- **確認D**: ルールを **「連番」(桁数1、開始100)** と **「日時」(作成日時)** の2つに
  作り直します。行の警告が **`作成日時不明・連番の桁不足`** のように**「・」区切り**で
  並び、どちらも読める。

### 手順3 — 行の警告が押しやすい(実機)

手順2の状態のままで見ます。

- **確認A**: 行の警告の文字を**指で押す**とダイアログが開く。
- **確認B**: **何度も外さないと押せない、ということが無い。** 文字の少し上下を押しても入る。
- **確認C**: 警告を押しても、**その行のチェックが切り替わらない**(選択が誤爆しない)。

### 手順4 — `（変更なし）` が出る

1. ルール設定シートを開き、トークンを**すべて削除**します。シートを閉じます。

- **確認A**: すべての行の変更後名が **`（変更なし）`** になっている。
  **`.png` や `.jpg` のような拡張子だけの名前が出ていない。**
- **確認B**: `（変更なし）` が**強調されていない**(赤でも緑でも青でもない、弱い色)。
- **確認C**: 「命名ルールが未設定です」の案内が出ている。
- **確認D**: トークンを**「元の名前」1つだけ**にします。**`（変更なし）` のままで**、
  未設定の案内は消える。
- **確認E**: 続けてトークンに「固定文字」を足すと、`（変更なし）` が消えて変更後名が出る。

## 結果の返し方

**自由形式で構いません。** 「手順2の確認Bが不成立、警告が左端に出ていた」のように、
**どの手順のどの確認か**と**見えたもの**を書いてください。スクリーンショットがあれば
助かりますが必須ではありません。

証拠のmetadata(対象commit、日付)はAgentが`task.md`へ記録します。

## 結果を受け取ったあとのAgent作業

1. 結果を`task.md`の観測済みcheckpointへ記録する(対象commitつき、**開発者の言葉をそのまま引用**)。
2. 不成立があれば、`T18`の欠陥か、既に引き受け先のある残余riskかを分ける。
3. 成立していれば最終証拠reviewを起動し、PASS後に`done`としてPRをmergeする。
