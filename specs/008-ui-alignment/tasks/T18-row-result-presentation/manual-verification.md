# 手動確認: 008:T18 行の結果表示

## 何を確かめてほしいか

自動testで閉じられなかったものだけを残してあります。**4項目、10〜15分**です。

| # | 確かめること | なぜ機械で閉じられないか |
|---|---|---|
| 1 | 変更後名の**緑と赤が実機で見分けられる** | 色の値はtestで固定できるが、実機の画面で区別できるかは見ないと分からない |
| 2 | 行の警告が**現在名の上に右寄せ**で出て、実機のフォントで読める | widget testはtest fontを使うので日本語の実寸が違う |
| 3 | **行の警告が押しやすい**(`008:T16`から引き受けた残余risk) | tap範囲の実サイズは実機でしか分からない。`T16`はemulatorでしか見ていない |
| 4 | ルール未設定と元名だけのルールで **`（変更なし）`** が出て、強調されていない | 実機の配色で「弱い色」に見えるかを見る |

**この確認で見たいのは「見分けられるか」「読めるか」「押せるか」です。** どの行が
警告対象かという判定は001が持っていて自動testで固定済みなので、内容の正しさを
突き合わせる必要はありません。

## 対象

| | |
|---|---|
| branch | `asdd/008-ui-alignment/T18-row-result-presentation` |
| code の最終commit | `e5aceed` |
| PR | #164 |

**これより新しいcommitは記録(`specs/`)とtestだけ**で、appの動きは変わりません。

**branchの切り替えは不要です。** Agentがこのbranchのまま待機し、結果を受け取るまで動かしません。

## 事前準備

起動手順は[`docs/development/emulator-verification.md`](../../../../docs/development/emulator-verification.md)に従います。

**このtaskはDartだけを変更しています。** 動いているappがあれば`R`(ホットリスタート)で足ります。

### 実端末でお願いします

**手順3(tap範囲)は実機でしか見られません。** `008:T16` のmanual確認をemulatorで
行ったため、tapの確かめが弱いまま残っており、**このtaskが引き受けています**
(2026-09-02の開発者の決定)。手順1・2・4はemulatorでも成立します。

### fixtureは`T07`のものを流用します

`.worktrees/t07-fixtures/` の画像24枚をそのまま使います。まだ端末に入っていれば送り直す
必要はありません。入っていなければrepositoryのルートで:

```powershell
adb push .worktrees/t07-fixtures /sdcard/DCIM/
```

## 手順

各手順の先頭でルールを作り直してください。**前の手順のルールが残っていると後半が成立しません。**

### 手順1 — 緑と赤が見分けられる

1. `DCIM/t07-fixtures` を読み込み、**全選択**にします。
2. ルールのトークンを**「固定文字」1つだけ**にします。値は `photo`。シートを閉じます。

- **確認A**: `.png` の24行の変更後名(`photo.png`)が**赤**、`.jpg` / `.txt` / `.pdf` の
  3行(`photo.jpg` など)が**緑**になっている。
- **確認B**: 赤と緑が**一目で見分けられる**。目を凝らさないと分からない、ということが無い。

> **なぜこうなるか**: 拡張子を保つので重複するのは`photo.png`の24枚だけです。他の3件が
> 正常なのは正しい動きです。

### 手順2 — 警告が現在名の上に右寄せで出る

手順1の状態のままで見ます。

- **確認A**: `.png` の行で、**現在名(`IMG_xxxx.png`)より上**に `名前が重複` が出ている。
- **確認B**: その文字が**行の右端側へ寄っている**。
- **確認C**: 文字が途中で切れて種別が読めない行が無い。
- **確認D**: ルールを **「連番」(桁数1、開始100)** と **「日時」(作成日時)** の2つに
  作り直します。行の警告が **`作成日時不明・連番の桁不足`** のように**「・」区切り**で
  並び、どちらも読める。

### 手順3 — 行の警告が押しやすい(実機)

手順2の状態のままで見ます。

- **確認A**: 行の警告の文字を**指で押す**とダイアログが開く。
- **確認B**: **何度も外さないと押せない、ということが無い。** 文字の少し上下を押しても入る。
- **確認C**: 警告を押しても、**その行のチェックが切り替わらない**(選択が誤爆しない)。

### 手順4 — `（変更なし）` が出る

1. ルール設定シートを開き、トークンを**すべて削除**します。シートを閉じます。

- **確認A**: すべての行の変更後名が **`（変更なし）`** になっている。
  **`.png` や `.jpg` のような拡張子だけの名前が出ていない。**
- **確認B**: `（変更なし）` が**強調されていない**(赤でも緑でも青でもない、弱い色)。
- **確認C**: 「命名ルールが未設定です」の案内が出ている。
- **確認D**: トークンを**「元の名前」1つだけ**にします。**`（変更なし）` のままで**、
  未設定の案内は消える。
- **確認E**: 続けてトークンに「固定文字」を足すと、`（変更なし）` が消えて変更後名が出る。

## 結果の返し方

**自由形式で構いません。** 「手順2の確認Bが不成立、警告が左端に出ていた」のように、
**どの手順のどの確認か**と**見えたもの**を書いてください。スクリーンショットがあれば
助かりますが必須ではありません。

証拠のmetadata(対象commit、日付)はAgentが`task.md`へ記録します。

## 結果を受け取ったあとのAgent作業

1. 結果を`task.md`の観測済みcheckpointへ記録する(対象commitつき、**開発者の言葉をそのまま引用**)。
2. 不成立があれば、`T18`の欠陥か、既に引き受け先のある残余riskかを分ける。
3. 成立していれば最終証拠reviewを起動し、PASS後に`done`としてPRをmergeする。

---

# 再確認(2026-09-03。2回目)

**1回目(対象commit `e5aceed`)で受領した指摘のうち2件を直したため、`lib/` が動きました。**
証拠は失効しますが、**やり直すのは手順2と手順3だけ**で足ります。

| | |
|---|---|
| code の最終commit | **`176d311`** |
| 変更した file | **`lib/ui/file_list/rename_warning_view.dart` の1本だけ** |

**手順1(緑と赤)と手順4(`（変更なし）`)は再確認不要です。** どちらも変更後名を描く
`_NewName`(`file_list_view.dart`)の話で、今回のdiffは**行の警告の widget しか触っていません**
(`git diff e5aceed..HEAD --name-only -- lib/` が1本だけを返すことで確かめられます)。

## 何を直したか

| 1回目の指摘(原文) | 直し方 |
|---|---|
| 「警告の先頭にある！マークが警告文に対して少し上にずれている。**修正したい**。」 | アイコンを箱の上端ではなく**文字のbaseline**へ揃えた。固定値で押し下げていないので、文字倍率が変わっても揃う |
| 「ぱっと見だと押せることが分からず、ただの警告文に見える。角を丸めた赤の四角で囲み、中をさらに薄い赤で塗りつぶしてボタンぽっくしても良いかも。」 | **角丸の枠 + 薄い塗り**を付けた。箱は中身の幅しか取らず、右寄せは外側の `Align` が担う |
| 「変更後名の表示の赤と同じ濃さなので、目が散る。少しだけ薄くしても良いかも。」 | 行の警告の文字とアイコンを、**`danger` の色相のまま濃さだけ下げた**(変更後名より薄い) |

## 手順2′ — 警告の見た目(5分)

1. `DCIM/t07-fixtures` を読み込み、**全選択**。トークンを**「固定文字」1つだけ**(値 `photo`)にします。

- **確認A**: `.png` の行の警告が、**角の丸い枠で囲まれ、中がうっすら赤く塗られている**。
- **確認B**: **押せそうに見える**。ただの文字には見えない。
- **確認C**: 警告の赤が**変更後名の赤より薄い**。並べて見て目が散らない。
- **確認D**: **！マークが文字と同じ高さに揃っている**。上へ浮いていない。
- **確認E**: 警告が**現在名の上に右寄せ**のままで、文字が切れていない。
- **確認F**: ルールを**「連番」(桁数1、開始100)+「日時」(作成日時)**へ作り直すと、
  `作成日時不明・連番の桁不足` が枠の中に収まって両方読める。**一覧が潰れていない。**

## 手順3′ — 押しやすさ(実機、3分)

- **確認A**: 枠の中を**指で押す**とダイアログが開く。
- **確認B**: 文字の少し外側(枠の内側)を押しても入る。
- **確認C**: 押しても**行のチェックが切り替わらない**。
- **確認D**: **枠の外(左側の空白)を押しても開かない。** 当たり判定は**枠の中だけ**である。

  > 1回目(`e5aceed`)は当たり判定が行幅いっぱいで、枠の左側の空白でも開いていました。
  > **2026-09-03 に開発者が「枠の中だけが反応でよい。むしろそっちの方が自然」と決めた**
  > ので、これは意図した振る舞いです。**見た目と当たり判定が一致していることを確かめる
  > 項目**であって、狭さを問う項目ではありません。

## 結果の返し方

1回目と同じで構いません。**どの確認か**と**見えたもの**を書いてください。

---

# 再確認(2026-09-04。3回目。**1項目・1分**)

**2回目で不成立だった手順2′の確認D(！マークの高さ)だけを見てください。**

| | |
|---|---|
| code の最終commit | **`9c2d6df`** |
| 変更した範囲 | **行の警告のアイコンの縦位置だけ** |

**手順2′のA〜C・E・Fと手順3′は `176d311` で成立済みなので、再確認不要です。**

## 何を直したか

`baseline` は揃っていましたが、**Material icons と CJK の字面(ink)の中心が 0.12em ずれる**ため
アイコンが上へ浮いて見えていました(開発者の見立て「テキストが枠に対して少しだけ下に位置
しているのに対し、！は枠の中央に位置している」と同じ話です)。font size に比例した量だけ
アイコンを下げました。

## 手順

1. `DCIM/t07-fixtures` を読み込み、**全選択**。トークンを**「固定文字」1つだけ**(値 `photo`)に。

- **確認A**: `.png` の行の警告で、**！マークと文字の高さが揃って見える**。上へ浮いていない。
- **確認B**: **下がりすぎてもいない**(！が文字より低く沈んでいない)。
- **確認C**: 端末の**フォントサイズを最大**にしても、揃いが大きく崩れない。確認後は既定へ戻す。

**「まだ少し気になる」程度なら、そのまま受容して閉じる選択もできます。** 判定は開発者に属します。

