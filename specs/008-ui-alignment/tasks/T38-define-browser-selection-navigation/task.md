# T38 app内browserの選択・戻る導線を定義する

## 目的

2026-09-22のT37エミュレータ確認報告で、範囲選択は概ね動く一方、全選択後の一括解除、画面を閉じる×の意味、選択行の表示、保存場所とfolderの戻り方が直感的でないと指摘された。004の承認済みの振る舞いを守りつつ、Android app内browserの操作導線を一つの設計へ整理し、必要な仕様変更を開発者の承認へかける。実装はT39が持つ。

## 入力と既存taskとの境界

- 観測: 2026-09-22のT37エミュレータ確認報告（会話）。「概ね機能」「TalkBack手順の意味が不明」「全選択はあるが全解除は無い」「headerの×が選択解除の×に見える」「checkboxをリネーム画面へ統一したい」。
- 開発者の案: footer左下に暗い背景・シアンの枠と文字の「リネーム画面に戻る」button、上部を保存場所名と左矢印にし、選択中は×と「〇件選択中」へ変える、現在地の帯へfolder一括選択checkboxを置く。
- 004 REQ-015/016/020: **保存場所が複数なら一覧から、1つだけならその保存場所のrootから始めること**、**複数あるときは閉じずに切り替えられること**、rootより上へ辿れないこと、folder移動で選択が消えること、全選択は現在folder直下のfileだけを対象にすることを保持する(**2026-09-22の`T11`で入口が変わり、近道は取りやめた**)。変更したい場合は仕様案を作り、承認前に実装しない。
- T11/T12: 保存場所1件時の入口、**近道の撤去**、戻る矢印の向き、空folder表示を持つ。T38はT11の承認済みの意味を前提に定義し、それらを複製しない。T39はT12の実装結果とも整合させる。
- T37: 長押しdragとedge自動scrollの意味を変えない。T38は選択controlと画面を閉じる操作の提示を持つ。
- desktopのOS pickerは対象外。

## 決めること

1. 一括解除をどこに置き、全選択と一対の操作として見せるか。候補は現在地帯の一括checkbox、headerの×、ケバブ。複数の同義操作を無造作に並べない。
2. headerの×を撤去してfooterへ「リネーム画面に戻る」を常時表示するか。閉じたときは現在と同じく未確定の選択を捨て、rename画面の既存状態を保持する。
3. 保存場所名を上部へ、現在folderの場所をその下へ置くか。左矢印はfolder内なら親folderへ、保存場所rootなら保存場所一覧へ戻るのかを区別し、rootより上のfilesystemへは移動しない。
4. 選択時に上部を「× + N件選択中」に変える場合、×は「現在folderの選択を全部解除」か「選択表示を閉じる」かを明確にし、画面を閉じる操作と誤認させない。
5. 「folder全体checkbox」は直下fileだけを選ぶのか、子folder以下も含むのか。REQ-020は前者を要求し、後者はfolder移動と同一folder選択の保証を変えるため、明示承認なしに採らない。
6. file行のcheckboxをメイン一覧T29の右端・円形・アクセント色に揃え、選択済み行の面色も揃えるか。browser固有のfolder行はnavigationとして識別できる形を保つ(**近道は2026-09-22の`T11`で取りやめた**)。
7. TalkBackを知らない開発者でも、manualのUI説明だけで全選択と一括解除の操作を再現できるか。

## 2026-09-23 の決定と操作状態表

### 開発者の決定

| 論点 | 決定 |
|---|---|
| `×` の意味 | **選択中だけ出し、「現在folderの選択をすべて解除」だけを意味する。** 画面は閉じない |
| 画面を閉じる導線 | **footer左下の「リネーム画面に戻る」に一本化**する。未確定の選択は捨て、rename画面の既存状態は保つ |
| 選択0件のheader | 同じ位置に **`←`(上へ)とルート(保存場所)の名前**だけを出す |
| 選択中のheader | 同じ位置が **`×` + 「N件選択中」** へ変わる。**`←`は消える**(上へ戻るには先に解除する) |
| 現在地の帯 | 戻るboutonを消し、**パンくず**にする(`内部ストレージ › Pictures › Screenshots`) |
| パンくずのtap移動 | **別taskへ分ける**(このtaskでは表示だけ)。選択中に上へ移動する手段は、それが入るまで無い |
| 「すべて選択」 | footerから外し、**headerの右端に固定したケバブ**へ入れる。**選択の有無にかかわらず常設**で、件数にも関わらず出し続けてよい |
| 「選択をすべて解除」 | **1件でも選択されているときだけ**ケバブへ出す |
| file行のcheckbox | メイン一覧(`T29`)へ揃える(右端・円・アクセント色)。選択済み行の面色も揃える |
| folder一括checkbox | **置かない**(「すべて選択」がケバブへ移ったため、現在地帯にcheckboxを持たせない) |

**`×`とケバブの「選択をすべて解除」は同義だが、意図して両方置く。** `×`は速い経路、ケバブは**操作名で認識できる経路**である
(004 REQ-020が全選択へ同じ理由で求めているもの)。**無造作な重複ではない**ことをここに記録する。

### 操作状態表

> **2026-09-23 の注記**: footerの「リネーム画面に戻る」は、`T39`のエミュレータ確認で開発者が**「← リネーム画面へ」**へ短くした(切れて読めなかったため)。意味(画面を閉じる唯一の導線)は変わらない。決定は`T39`の`task.md`の「人間の決定」。下の表は承認時の記録として書き換えない。

`←`と`×`は**同じ位置**を共有する。ケバブは**常に右端**にあり、位置が動かない。

| 現在地 | 選択 | header 左 | header 中央 | header 右 | 現在地の帯 | footer |
|---|---|---|---|---|---|---|
| 保存場所の一覧 | — | 無し | 「ファイルを選ぶ」 | ケバブ(「すべて選択」は**無効**) | 無し | 「リネーム画面に戻る」 / 「確定」(**無効**) |
| root(保存場所が複数) | 0件 | `←`(**保存場所の一覧へ**) | 保存場所名 | ケバブ | パンくず(保存場所名のみ) | 「リネーム画面に戻る」 / 「確定」(無効) |
| root(保存場所が複数) | 一部 | `×`(**全解除**) | 「N件選択中」 | ケバブ(「すべて選択」+「選択をすべて解除」) | 同上 | 「リネーム画面に戻る」 / 「確定」(**有効**) |
| root(保存場所が複数) | 全件 | `×` | 「N件選択中」 | 同上(「すべて選択」は**無効**) | 同上 | 同上 |
| root(保存場所が1件) | 0件 | **無し**(それより上へは辿れない) | 保存場所名 | ケバブ | 同上 | 「リネーム画面に戻る」 / 「確定」(無効) |
| root(保存場所が1件) | 一部 | `×`(**全解除**) | 「N件選択中」 | ケバブ(「すべて選択」+「選択をすべて解除」) | 同上 | 「リネーム画面に戻る」 / 「確定」(**有効**) |
| root(保存場所が1件) | 全件 | `×` | 「N件選択中」 | 同上(「すべて選択」は**無効**) | 同上 | 同上 |
| 下位folder | 0件 | `←`(**親folderへ**) | 保存場所名 | ケバブ | `保存場所名 › A › B` | 「リネーム画面に戻る」 / 「確定」(無効) |
| 下位folder | 一部 | `×`(**全解除**) | 「N件選択中」 | ケバブ(「すべて選択」+「選択をすべて解除」) | 同上 | 「リネーム画面に戻る」 / 「確定」(**有効**) |
| 下位folder | 全件 | `×` | 「N件選択中」 | 同上(「すべて選択」は**無効**) | 同上 | 同上 |
| 空のfolder | 0件 | `←` | 保存場所名 | ケバブ(「すべて選択」は**無効**) | 同上 | 「リネーム画面に戻る」 / 「確定」(無効) |

- **folderを移動すると選択は解除される**(004 REQ-016)。したがって移動後のheaderは必ず`←`側の形に戻る。
- **「すべて選択」の対象は現在folder直下のfileだけ**(004 REQ-020)。folderと、選べないentryは含めない。
- **「選択をすべて解除」の対象も現在folderの選択だけ**である(選択は同一folder内に限るため、それ以外に選択は存在しない)。
- **Androidのシステムバックは「リネーム画面に戻る」と同じ**(未確定の選択を捨てて閉じる)。
- **rootでもfileは選べる。** root直下にfileがあれば選択でき、そのときheader左は`×`になる — つまり
  **root(複数)では選択中に「保存場所の一覧へ」が消え、root(1件)では元から何も無い位置に`×`が出る**。
  保存場所を切り替えたいときは先に解除する(004 REQ-015が求める「閉じずに切り替えられる」は、
  **選択していない状態で成立していればよい** — folderを移れば選択は捨てられるため、切り替えと選択の保持は両立しない)。
- **`←`が無いのはroot(1件)の選択0件のときだけ**で、その他の行では`←`か`×`のどちらかが必ずその位置にある。

### reviewで触れた、直さないもの

`008:T37`の`manual-verification.md`には、近道を前提にしたfixtureと期待が残っている。**冒頭に
「入口と近道の期待は現行でない」注記があり**、`T11`の分類(doneしたtaskのmanualは**結果を書き換えず注記だけ**)に
従って**そのままにする**。再実行するときは注記に従って読み替える。

## 受け入れ証拠

- 操作状態表: 保存場所一覧、root、下位folder、選択0/一部/全件で、header・現在地帯・footer・戻る・一括選択/解除の意味が一意に読める。
- 004 REQ-015/016/020の維持・変更点をdiffで示し、変更が必要なら仕様と代表例の案を作って開発者の再承認を得る。
- 承認後、実装が守る仕様IDをT39の`task.json.covers`へ反映する。
- T11/T12との重複と実装順を整理し、T39へ観測可能なwidget testと、fixture・対象build・操作・期待結果まで書いた実機手順を着手前に引き渡す。
- ASDD workspace check PASS。仕様やproduct挙動の決定後に独立reviewを行う。review modelは開発者指定のluna。

## 独立review

**reviewerのmodelは`gpt-5.6-luna`**(開発者指定。実装(仕様定義)はClaude Opus 5)。

- attempt 1: `3a0c9fa..76a17bb`(+ status commit) — **FAIL**。
  - **P1(成果物の欠陥)**: 操作状態表が**rootの選択中**を定義していなかった。root直下にfileがあれば選択できるので
    到達不能ではなく、受け入れ証拠が求める「選択0/一部/全件が一意に読める」を満たしていなかった。
    → **root(複数) / root(1件)それぞれに一部・全件の行を足し**、`←`が消える条件と保存場所の切り替えとの関係を書いた。
  - **P2**: `plan.md`のtask一覧に`T40`が無かった(`plan.json`にはある)。→ 足した。
  - **P2(残余)**: `T37`の`manual-verification.md`に近道前提のfixtureが残っている。**`T11`の分類に従いそのままにする**
    (doneしたtaskのmanualは結果を書き換えず注記だけ)。判断を上の節へ記録した。
  - REQ-020の追記、代表例34・35、REQ-015〜019の維持、`×`の意味と`←`の代償の記録、提示を規範化していないこと、
    近道撤去との整合、workspace check、normative check、`lib/`・`test/`に差分が無いことは**いずれも確認された**。

- attempt 2: `3a0c9fa..6a10a5e` — **FAIL**。
  - **P1(成果物の欠陥)**: handoffに登録時(2026-09-22)の「候補UIは未承認」が残り、**2026-09-23の承認と矛盾していた**。
    `T39`が承認済み仕様を実装してよいかを誤読させる。→ **登録時の記録であることを明示し**、現在地を「承認済み、残るのは
    このtask自身のreviewだけ」と書き直した。Evidence revisionも承認の正本(task.md / plan.md / specのStatus行)を指すようにした。
  - attempt 1 のP1/P2(rootの選択中、`plan.md`のT40索引)は**閉じたと確認された**。REQ-020と代表例、`×`と`←`の記録、
    T40の登録と粒度、近道撤去との整合、各checkのPASSも再確認された。

- attempt 3: `3a0c9fa..8c9b25a` — **PASS**。P0/P1と安全網の穴なし。操作状態表の網羅、REQ-020の追記と代表例、
  `×`と`←`の代償の記録、`plan.json`/`plan.md`/T39依存/T40の粒度、近道の古い記録の扱い、承認状態の表現、
  workspace check / normative check / `git diff --check` / `lib`・`test`に差分が無いことを確認した。

**PASS後に1か所だけ直した(記録のみ)**: `plan.md`の`T38`の説明に「提案の配置・文言はこの時点では未承認である」が
残っていたので、**2026-09-23に承認された**ことを追記した。**仕様・状態表・引き渡しは変えていない。**

## Current state / handoff

- Last checkpoint: **完了**(2026-09-23)。操作状態表とUIの決定、004 REQ-020への一括解除の追加を開発者が承認し、独立review attempt 3 がPASSした。`T39`へ引き渡し、パンくずのtap移動は`T40`として新設した。
  - 登録時(2026-09-22)の記録: T37エミュレータ確認報告から論点を登録した。**その時点ではUI候補は未承認だった**(2026-09-23に承認された)。登録の独立reviewは `gpt-5.6-luna`、exact range `bef8337...edf4b08` でP2×3のFAIL、依存・証拠版・実機手順の引継ぎを修正後の `bef8337...5f31387` でPASS（P0〜P2なし）。**これは登録内容のreviewで、2026-09-23の決定のreviewではない。**
- Blocker category: なし(**T11は2026-09-22に承認済み**)。
- Evidence revision: base は `dev@3a0c9fa`。**決定と承認は2026-09-23の会話で受領し、このtask.mdと`plan.md`の「人間の決定」、004 specのStatus行が正本である。** 観測の出所(2026-09-22のT37エミュレータ確認報告)はT37の `f4b7868` に記録され、対象code/testは `d88ab4f`、T37はPR #184でdev@`180ab77`へ統合済み。
- Waiting for: `dev`へのmerge判断。
- Requested action: なし。
- Next Agent action: `dev`へmergeし、`T39`(実装)を着手可能として扱う。**`T40`は`T39`の後。**
