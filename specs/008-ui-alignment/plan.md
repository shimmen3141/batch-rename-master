# 008 UIと主要操作の整合

## 目的

実機で触って出た「操作の意味が読み取れない」指摘を解消する。並び順は現在の状態が見える一つのcontrolになり、リストからの除去は選択と混同されなくなり、tokenは設定を終えてから列に入る。参考designと現実装の食い違いのうち、**利用者の操作の意味に関わるもの**を揃える。

002/003の仕様は「視覚デザインは非規範」として見た目を対象外にしており、その結果これらの指摘がどの計画にも割り当てられないまま残っていた。008がその受け皿になる。

## 境界

### 対象

- 並び順controlの作り直しと、002 REQ-014(連番が無いとき手動並び替えを隠す)の廃止。
- 行のcheckbox(rename対象)と行の×(一覧から除去)の役割重複の解消。「すべて外す」の改名。
- token追加を「既定値で即追加してから編集」から「modalで設定を終えてから追加」へ変える。
- 行のサブ情報の情報階層。狭幅で必要な情報が読めなくなる問題を含む。**行のpreview**(画像・動画。取れないものは種別アイコン)とリッチな行layoutを含む。
- **警告の置き場所**。集約帯から各行へ移す(2026-08-27の決定)。002の行データと005の警告提示が変わる。
- 読み込み導線と場所の提示。
- **Androidのapp内file browserの提示**(入口、近道の見分け、上位へ戻る操作、行のpreview)。`013:T07`が画面を新設し、実機確認で指摘が出た。
- modalの文言と見せ方(結果の提示手段を含む)。
- リスト表示modeの切替。
- 全体の余白・階層・typography。
- 上記に伴う002/003/004 specの更新と、人間の再承認。

**このplanはUI調整の受け皿である。** 実機で触って出るUIの指摘は今後も増える前提で、新しい指摘は原則このplanへtaskとして足す。別planを立てるのは、UIの提示ではなく判定・契約・permissionが動くときに限る。

### 対象外

- ルールのpreset保存UI。→ 009
- 元名の大小変換。→ 001の将来拡張
- 001の重複判定をfolder単位へ変えること。→ 判定は001が正本。008は提示だけを扱う
- Androidのrename成功経路。→ 013

## 方針

- **仕様を変えるものは、仕様更新taskと実装taskを分ける。** 002/003/004はいずれもLightだがapprovedなので、外部から観測できる振る舞いを変えるには人間の再承認が要る。承認前に実装を始めない。
- **`covers`は仕様更新taskが埋める。** 全taskの`covers`は現在空である。REQ IDが確定するのはT01/T03/T05/**T11**/**T15**/**T17**の承認時なので、その6taskの受け入れ証拠に「対応する実装taskの`covers`を書く」ことを入れてある。T07/T08/T10/**T13/T14**は仕様を変えないため、空のままが正しい。
- **判定は動かさない。** 001の重複・桁不足・空名・基準日時不明の判定、005の実行可否、004の読み込み契約はそのまま。008が変えるのは**提示と操作の導線**である。
- 参考design `docs/design/Bulk Renamer.html` は配置・導線・情報階層の正本(AGENTS.md)。各taskは適用する画面範囲を`task.md`へ書く。
- 実機で触らないと判断できない項目があるため、実装taskはmanual確認を持つ。手順は実装後にcurrent revisionと照合して具体化する。
- 005:T09で下部固定バーへ集約した実行導線は動かさない。今回触るのは一覧の行、並び順control、token追加、警告の提示である。

## 全体の受け入れ条件

- [ ] 連番トークンの有無に関わらず手動並び替えができ、並び順controlが現在の状態(名前/作成日時/更新日時/サイズ/カスタム、昇順・降順)を常に示す
  - 証拠: 002 specの更新と再承認、仕様由来test、Windows desktopとAndroidでの手動確認
- [ ] 一覧からの除去がcheckboxと混同されない導線になり、「すべて外す」が何をするか名前から分かる
  - 証拠: 002/004 specの更新と再承認、仕様由来test、手動確認
- [ ] tokenは設定を終えてから列に入る。既定値のままのtokenが紛れない
  - 証拠: 003 specの更新と再承認、仕様由来test、手動確認
- [ ] 狭幅でも、どの行の作成日時が不明かが読み取れる
  - 証拠: widget test、Android実機での手動確認(`T07`)
- [ ] 行から、その file の中身(画像・動画)と種別が分かる
  - 証拠: widget test、unit test、Android実機での手動確認(`T07`)
- [ ] **どのファイルが何を理由に警告されているかを行から読み取れ、件数が多くても一覧が潰れない**
  - 証拠: 002/005 specの更新と再承認(`T15`)、widget test、Android実機での手動確認(`T16`)
- [ ] 読み込み前・読み込み後・複数folderが混ざった状態で、どこから何が入っているかが読み取れる
  - 証拠: widget test、AndroidとWindows desktopでの手動確認
- [ ] リスト表示modeを切り替えても、選択・手動並び替え・警告表示が成立する
  - 証拠: widget test、両platformでの手動確認
- [ ] 余白と文字の階層がapp全体で一貫し、値がthemeへ寄っている
  - 証拠: 既存widget testの継続PASS、diff、両platformでの手動確認
- [ ] Androidのapp内file browserで、どこから読み込もうとしているか・近道と実体のfolderの違い・ファイルの中身が読み取れる
  - 証拠: 004 specの更新と再承認(入口と近道)、widget test、Android実機での手動確認。**`013:T07`が実機で確かめた要求(階層、選択、注記、rootの上限)を弱めていないこと**
- [ ] modalが、何を聞かれているか・実行すると何が起きるか・結果がどうなったかを、件数が多いときにも読み取れる
  - 証拠: widget test、Android実機での手動確認
- [ ] 警告が**個々のファイルの側だけ**に集まり、行を見れば結果と原因が分かる。ルール単位の常設表示と行の表示へ同じ内容が二重に出ない
  - 証拠: 002/005 specの更新と再承認(`T17`)、widget test、Android実機での手動確認(`T18`/`T19`/`T20`)
- [ ] `python <asdd-plugin>/scripts/workspace.py check specs`、`flutter test`、`flutter analyze`、`dart format --output=none --set-exit-if-changed .` がPASS
- [ ] 001の判定、004の読み込み契約、005の実行・undo・警告の判定を変えていない(既存testの継続PASS)

## 人間の決定

| 日付 | 論点 | 決定 | 決定者 |
|---|---|---|---|
| 2026-08-05 | 実施順 | 警告表示・進捗・undoのUIは005が作るため、008は005完了後に行う | 開発者 |
| 2026-08-05 | 並び順control | 横並びchipをやめ、現在の状態を示すドロップダウンにする。手動で並び替えた瞬間に「カスタム」へ変える。各keyへ昇順・降順を持たせる。あわせて002 REQ-014を廃止し、連番の有無に関わらず手動並び替えを可能にする | 開発者 |
| 2026-08-05 | 選択と削除 | 行のcheckboxと行の×の併存をやめ、checkboxへ統一する。削除は左swipeで出す。「すべて外す」は「リストを空にする」等へ改名する | 開発者 |
| 2026-08-05 | token追加 | 「既定値で即追加してから編集」をやめ、追加を押すとmodalが出て設定を終えると追加される形にする | 開発者 |
| 2026-08-12 | 行サブ情報の見切れ | 狭幅で`作成日時: 不明`の文字列が読めない件を、UI調整として008で扱う(識別自体は警告アイコンで成立しており仕様違反ではない) | 開発者 |
| 2026-08-13 | (a)〜(d)の扱い | 他のplanが拾わないため**008の対象へ入れる**。T07へ(b)を統合し、(c)(a)(d)をT08/T09/T10として足す。あわせて008を今後のUI調整の受け皿と位置づける | 開発者 |
| 2026-08-15 | 再採番結果の提示方法 | **`013:T11`が結果toastへ入れた「旧 → 新」の全件表示を、008で見直す。** 件数が多いとtoastが縦に伸びる(現在は高さ96pxで打ち切ってscroll)。**modalの方が向いている**という指摘を受けたが、005 contract REQ-024が求めるのは「どの項目がどの名前になったかを示す」ことで、提示手段は自由(005 spec「自由とする点」)。**振る舞いは変えず提示手段だけを動かすので008の範囲**である | 開発者 |
| 2026-08-25 | `013:T07`のUI指摘 | app内file browserの実機確認で出たU1〜U5を**008へtask化する**(T11〜T14)。U6(「すべて」を開き直したときに前回の場所と選択を復元する)は本人が「ふとした思い付き」としたため`product-map.md`の将来候補へ置く | 開発者 |
| 2026-08-27 | 行のpreview | `T07`の種別アイコンを**画像・動画のpreview**へ変える(スマホのファイルアプリのようなもの)。取れないものは種別アイコンへ落とす。文書・PDFのpreviewは出さない。**preview基盤(port・cache・遅延読み込み)は`T07`が作り、`T13`はそれをbrowserの行へ繋ぐだけにする** — 画面は違うが仕組みは同じで、二度作るのを避けるため | 開発者 |
| 2026-08-27 | 警告の置き場所 | **警告を集約帯から各行へ移す。** 「警告は各ファイルと一緒にあった方が、どのファイルが何の原因で警告されているか分かりやすい」。各ファイルの警告を簡潔な形で1〜2行に列挙し(例: ファイル名重複・ファイル名が空・連番の桁不足・作成日時不明)、クリックするとトグルかmodalで詳しい一覧が見られる。ファイル名が空なら重複警告は行に出さなくてよい(詳細では出す)。位置は参考designの警告表示の場所。**002の行データと005の警告提示・REQ-021のまとめ規則が変わるため、`T15`(仕様更新)と`T16`(実装)へ分ける** | 開発者 |
| 2026-08-27 | `T07`の範囲 | `T07`は**行のlayoutとpreviewで閉じる**。警告の提示は`T15`/`T16`が引き受ける。`T07`が入れた「内訳の高さを画面に応じる」は残す(小さい画面では従来値が下限) | 開発者 |
| 2026-08-29 | 桁不足の置き場所 | 連番の桁不足は 001 の `DigitShortageWarning` が**対象fileを持たない**ルール単位の警告なので、行へは出さず**ルール設定の導線と同じまとまり**へ出す。**実行は止めない** — 001の生成規則では数字が伸びるだけで名前は失われず、強制実行を選べば`autoResolve`が桁をそろえる。止めると001 REQ-011の桁拡張が使われる経路が無くなる **(配置の部分は8.0で仕様から降ろした。`T16`への設計指針として残る。「実行を止めない」は決定のまま)** | 開発者 |
| 2026-08-29 | 空名の出し方 | 空名は**原因がルール側・結果がfile単位**なので分ける。行は**(i)変更後名が空になること (ii)そのfileが改名の対象にならないこと**の2つ、原因はルール設定の導線へ一度だけ。**Androidでは作成日時が取れないため、作成日時トークンだけのルールで全件が空名になる**(現在は同じ原因をfile件数ぶん繰り返している) **(配置の部分は8.0で仕様から降ろした。`T16`への設計指針として残る)** | 開発者 |
| 2026-08-29 | 集約帯 | **廃止**する(**これは決定のまま。`T16`が拘束として受ける**)。ヘッダーの件数(`⚠ N 件の問題`)とmodalへ移す形は**8.0で仕様から降ろし、`T16`への設計指針として残る**。modalは**ヘッダーからも行の警告文からも**開く。一覧の常時の占有が0になり、`T07`の残余risk N-9(帯が一覧の半分以上を覆う)が消える | 開発者 |
| 2026-08-29 | 作成日時が取れないときの改名 | **振る舞いを変えない**(001 INV-006を維持)。更新日時での代替は、改名が不可逆で痕跡を残すため採らない — できた名前が撮影日かcopy日かを後から区別できない。skip案は`010`が撮影日時を供給した後の頻度を見てから判断する(product-mapへ登録) | 開発者 |
| 2026-08-29 | ソート文脈の警告 | 開発者が案を示した(原文: 「今後ソートはプルダウンにして右にスペースが空くと思うので、そこに作成日時ソート**選択時**の警告(**更新日時で代替することも含め**)を表示するのはどうでしょうか。」)。`T15`はこれを受けて**002 REQ-011を行へ移さない**と判断した(行が示すのは*そのfileの値が無い*こと、REQ-011が示すのは*一覧全体の並び順が別のkeyで決まっている*ことで粒度が違う)。**配置は`T01`が持つ** | Agent(開発者の案に基づく) |
| 2026-08-29 | 002/005 の再承認(**1回目。取り直しが必要**) | **002 REQ-015(行データが警告を持つ)と 005 contract revision 7.0(REQ-009の提示場所3条件・REQ-021のまとめ規則2つ・REQ-010)を承認。** 判定・自動解決・実行の振る舞いは不変。**この承認後に独立reviewが2回FAILし、REQ-009を種別ごとの表へ書き換えた**(承認時の文より弱くなる修正を一度入れてしまったため)。**承認を取り直した(下行)** | 開発者 |
| 2026-08-29 | 002/005 の再承認(**2回目。revision 7.0 の承認。下行の 8.0 が置き換えた**) | **REQ-009を「警告の種別ごとに、行・まとまり・詳細へ何を載せるか」の表へ書き換えた版を承認。** 行はトークンを名指しせず、名指しはまとまりと詳細が担う。あわせて 002 へ REQ-013(sortModeで強調をゲート)との境界を明記した — REQ-013が縛るのは日時表示そのもので、ルール文脈の警告表示は`sortMode`に依存しない。**1回目の承認後にAgentが要求を弱めてしまったため取り直した**(AGENTS.md「承認済みの仕様をAgentが自分で緩めない」) | 開発者 |
| 2026-08-29 | 提示の場所を仕様にしない(**案B**) | 独立reviewが**3回FAIL**し、原因が3回とも**配置を自然文で書き切ろうとしたこと**(経路の組合せ・形容詞の強度・layoutの分岐)だったため、路線を変えた。**005も002も元から配置を「自由とする点」へ置いていた**(005「提示方法(文言・配置・UI部品)」/ 002「視覚デザインは非規範」)ので、**revision 8.0 はその境界への復帰である**。REQ-009 は場所ではなく**利用者から何が読めるか**を課す: (1)一覧を見た状態で各ファイルの警告**種別**が分かる (2)ファイルによって変わらない説明は件数ぶん繰り返さない (3)全件と説明へ到達できる。**7.0 が防ごうとした退行は (1)(2) が防ぐ。** 配置は`T16`の裁量へ戻る — **上の3行(桁不足の置き場所・空名の出し方・集約帯)のうち、配置に関する部分は決定ではなく`T16`への設計指針として残る。集約帯の廃止だけは決定のままである。** **revision 8.0 を承認**(2026-08-29) | 開発者 |
| 2026-09-02 | `T16`のmanual確認と改善要望 | **`T16`はここで閉じ、同時に受領したUIの改善要望14件は新taskへ分ける。** manual確認は5項目すべて成立し、`T07`から引き受けた残余risk N-9 は閉じた。要望のうち**警告をファイル単位へ一本化する案(桁不足を該当行へ、ルール単位の警告を廃止)と、行から開く詳細をその行だけにする案**は承認済みの正本と衝突するので、**仕様更新taskと実装taskへ分ける**(この方針は上の「仕様を変えるものは分ける」の適用である) | 開発者 |
| 2026-09-02 | 上の分解 | **task IDへの割り付けはAgentが行った** — `T17`(仕様更新)/`T18`(行)/`T19`(詳細modal)/`T20`(下部バー)を新設し、要望11・12を`T08`、要望10・13を`T10`へ足した。**画面部品で割ったのは、同じwidgetを複数のtaskが触る形を避けるため**である。衝突する正本の所在は、要望7が **002 REQ-015**(ルール全体への警告は行データに含めない)と 005 **spec** の例20c、要望2が 005 **spec** の例20e、要望1が **002 REQ-006** と **005 REQ-019** である。**(この段落は 2026-09-02 時点の判断であり、同日の独立reviewが覆した。下の「承認の取り直しが必要になった理由」行を読むこと。)** 当時の判断: 005 contract の REQ-009 とは衝突しない — (1)は桁不足を許容し、(3)は経路の数を課していない。硬い衝突は 002 spec 側にある | Agent(開発者の決定に基づく分解) |
| 2026-09-02 | 手動確認の実施環境 | **`T16`のmanual確認をAndroid実機ではなくemulatorで行ったことを受容する。** 差が出るのは手順4の確認E(tap範囲が「押しやすい」)だけで、**行の警告のtap targetは要望2で作り直すため、実機でのtap確認は`T18`へ送る**。Agentが`manual-verification.md`で受け入れ証拠を独断で「emulatorで可」へ緩めていたことは、独立review attempt 7 の指摘として記録済み | 開発者 |
| 2026-09-02 | 要望8の対象 | **「リネーム前の名前と同じ行の右へ右寄せ」の対象は「行の警告」である。** 原文の「各行のファイルは」が一意に決まらず、(a)行の警告 (b)ファイルの場所 (c)サブ情報全般 の3案を示して確認した。参考designも `warnText` を上段右へ、`meta`(サイズ・日時)を下段へ置いており、この読みと整合する。**サブ情報は動かさず、ファイルの場所は要望11のとおり`T08`が扱う** | 開発者 |
| 2026-09-02 | 変更が0件のときの実行 | **変更が生じるファイルが0件なら実行buttonを押せない**(参考design準拠)。提示した3案は(a)変更0件なら押せない (b)押せるままで結果の出し方を直す (c)元名のみのルールという形で特別扱いする で、**(a)を選んだ**。**判定はルールの形ではなく結果で行う** — `[元の名前]`1つだけのルールも、同じ結果になる別の形も同じ扱いになる | 開発者 |
| 2026-09-02 | 005 revision 9.0 と 002 REQ-015 の再承認(**1回目。取り直しが必要**) | **一度承認したが、直後の独立reviewが2つのP1を見つけたため取り直す**(下行)。承認時の案は次のとおり。 005 は用語「変更が生じるファイル」を足し、**REQ-019**を0件条件へ広げ、**REQ-029**(変更が生じないことが読める)を加え、SM-001のguardを揃えた。**REQ-009 は変えていない** — (1)は桁不足を免除しているだけで行への提示を禁じておらず、(3)は経路の本数を課していない。002 は **REQ-015** を「行データが桁不足の対象も導出して持つ」へ改めた(001が返しているときだけ導出し、返っているなら該当は必ず1件以上)。**REQ-006は変えない。** あわせて**代表例21の振る舞いが変わることを承認した** — 全選択ファイルが REQ-022 の除外に当たる場合、変更が生じるファイルが0件なので**強制実行の経路へ入らなくなる**(除外の理由は REQ-009 の提示から読めるため、読めなくなる情報は無い) | 開発者承認 |
| 2026-09-02 | 承認の取り直しが必要になった理由 | **(1) 要望7を義務づける正本が無かった。** REQ-009 を変えずに済むという当初の判断は「桁不足を行に出してよい」の向きにしか成り立たず、**「出さねばならない」を課す must がどこにも無かった**(002 REQ-015 は行データが持つことしか課さず、005 spec の代表例は正誤判定を行わない)。REQ-009 (1) の免除条項を狭め、**(4)**(ファイルごとの入口からはそのファイル、全件は絞られない入口から)を足した。**(2) 例21a の記述が事実に反していた。** 8.0 は「全件が除外」ではなく、**同一folderに空ベース名が2件以上あると自動解決が2件目以降へ ` (1)` を付けて改名していた**。9.0 が止めるのは「利用者が確認していない改名」であり、承認時の説明より**実質の大きい変更**である | Agent(独立review attempt 1 の指摘) |
| 2026-09-02 | 005 revision 9.0 と 002 REQ-015 の再承認(**2回目。訂正版**) | **承認。** 上行の2つのP1を直した版である。**REQ-009 を変えた** — (1)の免除条項を「連番の桁不足も、その連番が指定桁数を超えて描かれるファイルについては同じように種別が読める」へ狭め、**(4)**(ファイルごとの入口からはそのファイルの警告、全件は絞られない入口から、範囲が混ざらない)を足した。**場所・文言・UI部品には踏み込んでいない** — 課しているのは範囲であって配置ではなく、revision 8.0 の境界を越えていない。あわせて**例21aの実質を承認した** — 8.0 では同一folderに空ベース名が2件以上あると2件目以降が自動解決で ` (1).ext` へ改名されており、**9.0 が止めるのはこの「利用者が確認していない改名」である** | 開発者承認 |
| 2026-09-02 | `T17`の独立reviewが3回FAILしたときの進め方 | **残る2件へ注記を足して attempt 4 へ進み、「写しが古くなる型」の仕組み化は別taskへ切り出す**(案A)。3回FAILで`blocked`にして返した3案は、(A)この案 (B)経緯の記述を`task.md`・`plan.md`から構造的に排除する枠組みへ変える (C)記録の追随漏れを独立reviewのFAIL条件から外す、であった。**3回とも規範本文は正しく、落ちたのは一貫して「同じ主張を複数の文書へ散文で持ち、改訂時に一部だけ直す」型**だったため、仕様側をやり直す理由が無いと判断した。**切り出し先は`product-map.md`の将来候補へ登録した**(着手判断の前にplanへ定義しない) | 開発者 |
| 2026-09-03 | `T18`のmanual確認と指摘の扱い | **手順1〜4がAndroid実機ですべて成立。** あわせて3件の指摘を受領した。**(1)「！マークが警告文に対して少し上にずれている。修正したい」と (2)「押せることが分からず、ただの警告文に見える…しても良いかも」「変更後名の赤と同じ濃さなので目が散る。少しだけ薄くしても良いかも」を、まとめて`T18`で直す** — (1) の修正で `lib/` が動き実機証拠が失効するので、同じwidgetの件を一度に直して再確認を1回にまとめるほうが安い。**(3)「元の名前だけを設定していてもリネームボタンはグレーアウトされていなかった」は`T18`の欠陥ではなく`T20`の範囲**である(005 REQ-019 revision 9.0。`T20`の`covers`にある)。**(2) は「〜しても良いかも」で断定されていない** — 断定へ強めず、選択肢として尋ねたうえでの決定である | 開発者 |
| 2026-09-03 | 行の警告の当たり判定 | **枠の中だけを反応させる。** `T18`が警告をバッジの形へ作り替えたとき、当たり判定が行幅いっぱいからバッジの幅へ縮んだ(以前は枠の外の空白でも開いた)。原文は「**枠の中だけが反応でよいです。むしろそっちの方が自然です。**」**見た目と当たり判定が一致する形**を選んだので、行全体を押せるようにする案・枠の余白を増やす案は採らない | 開発者 |
| 2026-09-04 | `T18`の独立reviewが3回FAILしたときの進め方 | **P1×3と確認D(！マークの高さ)を直して attempt 4 へ進む。** 3回FAILで`blocked`にして返した選択肢のうち、この案を選んだ。**3回とも実装の誤りは0件**で、落ちたのは一貫して**記録**である(M180の無言の失効、PR本文が古いmutation件数を主張、handoffが本文と矛盾)。`T17`の 2026-09-02 行と同じ型であり、切り出し先も同じ`product-map.md`の将来候補である | 開発者 |
| 2026-09-04 | `T18`を独立reviewのPASSを待たずに完了させる | **`T18`を`done`にして PR #164 を merge する。auto-merge条件2(exact rangeの独立reviewがPASS)を開発者の判断で免除した。** 独立reviewは5回連続FAILで、**5回とも実装の誤りは0件**。attempt 1〜4 は実質(testの空振り、manual証拠の失効、`M180`の無言の失効、未回答のmanual項目を機械で埋めたことにしていた)を捕まえたが、**attempt 5 は記録の整合だけ**で、成果物にも安全網にも影響しなかった。開発者の言葉は「**タスクに進む前にタスクのレビューで落ちていては本末転倒**」。実装・768 test・mutation 23 KILLED・実機manual全項目・CI・001の判定不変は**2人のreviewerが独立に再現済み**で、attempt 5 の指摘4件はすべて修正済みである。**未解決の項目はすべて引き受け先つきで記録した** | 開発者 |
| 2026-08-13 | 計画の承認 | 状態 draft → **approved**。task分割、対象外の範囲、(a)〜(d)を含めた構成に個別異議なし | 開発者承認 |

出典: `specs/product-map.md`の「008へ引き継いだ人間の決定(planへ反映済み)」節。原文は凍結した[`specs/history/asdd-0.x-discovery.md`](../history/asdd-0.x-discovery.md)の44〜48行。

## review記録

- Review attempt 1: `ea1dd04..d9d6bb2` — FAIL — P1: T09の`dependsOn`がtask.md本文の前提(T02によるREQ-014廃止)を宣言していない。P2×6: 引用節名の陳腐化、`008-ui-polish` slugの残存、`file_source_bar.dart`の担当重複、T07/T08の場所の提示の分担未定、`covers`を埋める時期の未定義、product-mapの到達点が古い。
- Review attempt 3(T11〜T14の登録): `ae59859...8726de6` — FAIL — **P1-1: `T14`のmodal一覧の4行目が誤り。** `rule_builder_workspace.dart:76`を「tokenの追加 / `T05`/`T06`が持つ」と書いたが、実体は**ルール構築画面まるごとのbottom sheet**(mobileの下部バー「ルール設定」)で、`T05`/`T06`はこれを持っていない。**Androidではルールの編集が必ずこのsheet越し**なので、U5がこれを指していた場合に**どのtaskも拾わないまま落ちる**ところだった。P2×5: 種類選択sheetの分担を片側だけで宣言、`covers`の方針段落が新taskに未追随、境界に足した2行に対応する全体の受け入れ条件が無い、`T13→T12`の依存にした理由が未記載、`T11`のU2に「そもそも要求を足さない」案が無い。**すべて直した**(下の日付の決定表を参照)。
- Review attempt 4(同上、修正後): `ae59859...2c2339b` — **PASS** — 未解決P0/P1なし、**auto-merge 7条件すべて充足**。新規P2×3(`T14`のmanualが新しい分担に未追随、`T08`が`T14`の旧文言を現在形で引用、「Androidでは必ずsheet越し」は platform ではなく**幅**(`breakpoint` = 840dp)で決まる)も**merge前に直した**。
- Review attempt 2: `ea1dd04..72dd5f8` — PASS — 未解決P0/P1なし。残P2×3(行widgetのT04/T07/T09間の分担未宣言、T08 task.mdの内部矛盾、product-mapのlive status寄り記述)は`ec2e74f`のmerge後に解消した。

## タスク

タスクのID・依存・状態は`plan.json`と各`tasks/*/task.json`が正本。詳細は各`task.md`を読む。番号は安定した識別子であり、実行順や優先順位ではない。

| ID | 詳細 |
|---|---|
| T01 | [task.md](tasks/T01-define-sort-control/task.md) |
| T02 | [task.md](tasks/T02-implement-sort-control/task.md) |
| T03 | [task.md](tasks/T03-define-selection-flow/task.md) |
| T04 | [task.md](tasks/T04-implement-selection-flow/task.md) |
| T05 | [task.md](tasks/T05-define-token-add-modal/task.md) |
| T06 | [task.md](tasks/T06-implement-token-add-modal/task.md) |
| T07 | [task.md](tasks/T07-row-and-warning-presentation/task.md) |
| T08 | [task.md](tasks/T08-load-affordance-and-path/task.md) |
| T09 | [task.md](tasks/T09-list-display-modes/task.md) |
| T10 | [task.md](tasks/T10-spacing-and-typography/task.md) |
| T11 | [task.md](tasks/T11-define-browser-entry-and-shortcuts/task.md) |
| T12 | [task.md](tasks/T12-implement-browser-presentation/task.md) |
| T13 | [task.md](tasks/T13-browser-file-preview/task.md) |
| T14 | [task.md](tasks/T14-modal-wording-and-presentation/task.md) |
| T15 | [task.md](tasks/T15-define-row-level-warnings/task.md) |
| T16 | [task.md](tasks/T16-implement-row-level-warnings/task.md) |
| T17 | [task.md](tasks/T17-define-file-scoped-warnings/task.md) |
| T18 | [task.md](tasks/T18-row-result-presentation/task.md) |
| T19 | [task.md](tasks/T19-warning-detail-modal/task.md) |
| T20 | [task.md](tasks/T20-rule-and-exec-bar/task.md) |
