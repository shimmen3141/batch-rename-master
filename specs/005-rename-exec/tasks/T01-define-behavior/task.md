# T01 振る舞い仕様の作成(Strict)

## 目的

- 振る舞い仕様の作成(Strict)

## 入力と依存

- 依存: なし
- 同じworkspaceの`spec.md`、contract、decisions、仕様由来テスト。

## 変更範囲

- 旧planで定めた成果範囲。詳細は凍結履歴を参照する。

## 受け入れ証拠

- 「部分失敗の扱い」の回答が仕様に反映されている(未回答なら着手せずブロック報告)
  - `spec_lint.py --strict` が PASS(出力をログに添付)
  - spec.md の「反証ログ」に、実施した反証観点と検出した問題・対処が記録されている(0件ならその旨を明記)
  - 「境界 > 外部依存(ポート)が実際に供給できる値」に、`saf_util.rename` が**ドキュメント URI 経由では `name` を空で返す**ことと、**改名で URI が変わる**ことが出典つきで書かれている(fake が本物より情報豊富にならないようにするため)
  - 「この機能だけでは未完成な点」に、警告の見た目(008)と作成日時の取得経路(010)が送り先つきで書かれている
  - 仕様が `draft` 状態で作成され、完了報告にレビュー依頼が含まれている(approved 化は人間が行う。**後続タスクは仕様が approved になるまで実行不可**)
- 参考: create-verifiable-spec skill、`specs/004-file-source/decisions/ADR-001-file-source-plugins.md` の「実機で確認した事実」、001 の `contracts/behavior-contract.json`(REQ-007〜009 / REQ-014 / OP-004)

## 作業記録

旧ASDD 0.xから、実際に記録されていたcheckpointだけを移行した。将来予定やlive状態には使わない。

- 2026-08-06 / 着手 / 担当: shimmen3141
  - 2026-08-06 / done / verifier PASS(試行1) / verifier 所見: REQ-005 と INV-003/REQ-006 の相互作用が未定義だった点を、一時名は成功として記録しないと定めて解消。仕様は draft で人間の承認待ち
  - 2026-08-07 / 仕様承認 / 開発者承認(「承認します」)。未解決事項2件を解消: 巻き戻し期限=5秒、停止時の一時名は1件だけ元の名前へ戻す
