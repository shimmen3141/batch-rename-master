# T04 実行導線: 確認ダイアログ(キャンセル / 強制実行=autoResolve)+ 結果の提示

## 目的

- 実行導線: 確認ダイアログ(キャンセル / 強制実行=autoResolve)+ 結果の提示

## 入力と依存

- 依存: T02, T03
- 同じworkspaceの`spec.md`、contract、decisions、仕様由来テスト。

## 変更範囲

- 旧planで定めた成果範囲。詳細は凍結履歴を参照する。

## 受け入れ証拠

- 警告があるとき、キャンセルと強制実行を選べる。**強制実行は `autoResolve` の結果で実行する**
  - **ダイアログに警告の全件**(対象ファイル・該当トークン)が出る。一覧側の警告帯は既定で折りたたみなので、実行前に全部を見られる場所がここしか無い(開発者要望 2026-08-07)
  - 警告が無いときの実行経路も検証されている
  - 実行後、成功件数・失敗件数と失敗の理由が提示される
  - 実行中に二重で開始できない
- 参考: 001 の `autoResolve`(OP-004)、`lib/ui/file_source/file_source_bar.dart`(SnackBar の出し方)

## 作業記録

旧ASDD 0.xから、実際に記録されていたcheckpointだけを移行した。将来予定やlive状態には使わない。

- 2026-08-07 / T8 の仕様更新により REQ-022(空名になるファイルを実行対象から除外)が T4 の担当に加わった。T8 の仕様が再承認されるまで T4 も着手不可
