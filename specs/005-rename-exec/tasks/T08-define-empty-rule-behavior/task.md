# T08 空ルール時の扱いの仕様更新(実行不可・警告の代わりに未設定を提示)→ 再承認依頼

## 目的

- 空ルール時の扱いの仕様更新(実行不可・警告の代わりに未設定を提示)→ 再承認依頼

## 入力と依存

- 依存: T01
- 同じworkspaceの`spec.md`、contract、decisions、仕様由来テスト。

## 変更範囲

- 旧planで定めた成果範囲。詳細は凍結履歴を参照する。

## 受け入れ証拠

- **ルールが空のときは実行できない**ことと、**そのとき警告の代わりに「ルール未設定」を提示する**ことが要件として書かれている
  - **REQ-009 に例外が明記**されている(現状の REQ-009 は「警告 4 種すべてを提示する」なので、空ルール時の抑制はこのままでは違反になる)
  - SM-001 に、ルールが空のときは `requestExecute` で `running` にも `confirming` にも入らないことが反映されている
  - `spec_lint.py --strict` が PASS。反証ログに今回の更新分の観点が追記されている
  - 仕様が `draft` に戻され、完了報告に**再承認の依頼**が含まれている(**T9 は再承認まで実行不可**)
- 参考: 001 の `validate`(空ルールでは空名と重複の警告が正当に出る)、`docs/design/Bulk Renamer.html`

## 作業記録

旧ASDD 0.xから、実際に記録されていたcheckpointだけを移行した。将来予定やlive状態には使わない。

- 2026-08-07 / 着手 / 担当: shimmen3141
  - 2026-08-07 / done / verifier PASS(試行1) / verifier 指摘: SM-001 の ruleChanged が宣言だけで遷移が無く、REQ-020 の片方向しか状態機械に現れていなかった。2遷移を追加して解消
  - 2026-08-07 / 仕様の再承認 / 開発者承認(「T8は承認します」)。REQ-019〜022 を含む契約を draft → approved。T4 の着手ブロックも解除
