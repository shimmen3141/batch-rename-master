# 005 安全なリネーム実行

## 目的

001〜004 で「何に変えるか」は決まり、画面にも出る。しかし**実ファイルは一度も書き換えていない**。005 がその最後の一歩を担う。

同時に、001 の `validate` が出している警告(重複・桁不足・空名・基準日時不明)は**誰も表示していない**。仕様上は正しいが、利用者から見ると「作成日時トークンを入れたのに何も出ず、理由も分からない」状態で、実質壊れて見える。警告表示は 005 の責務と決めてある(2026-08-02 / 2026-08-05 開発者決定)ので、実行と同じ計画で塞ぐ。

この機能だけが**取り返しのつかない操作**を持つ。ファイル名は利用者の資産で、失敗しても Ctrl+Z は無い。そのため仕様は Strict とし、正しさを契約で固定してから実装する。

## 境界

### 対象

- `spec.md`、contract、既存テストが定めるこの機能の成果。
- 各タスクに移行した実装・検証・未完了受け入れ。

### 対象外

- 永続的な undo 履歴スタック(セッションを跨ぐ巻き戻し)。→ 意図的に不要
- ファイルの移動・コピー・削除。005 は**改名のみ**を行う。→ 意図的に不要
- Android で更新日時を書き換えること(SAF に API が無い)。→ 意図的に不要
- 撮影日時など作成日時の取得経路の追加(001 REQ-010 の範囲)。→ 010-photo-source
- 警告の見た目の作り込み(配色・レイアウト・アニメーション)。→ 008-ui-alignment

## 方針

- **ポートを1つ増やす**: `RenameExecutor.rename(handle, newName)` → 成功またはstableな失敗結果。desktopはOS固有の排他的native renameで既存targetを置換しない。Android SAFは原子的no-replaceを保証できないため、PR #116のrevision 2ではprovider APIを呼ばず理由付きunsupportedを返す。Androidの成功経路は013でstorage・permission境界から設計する。
- **ハンドルは改名のたびに更新する**。T8 の実機スパイクで、SAF は**改名すると URI が変わる**ことが確認されている(ADR-001)。更新しないと連続実行と「元に戻す」が壊れる。戻り値の `name` はドキュメント URI 経由では空だったので、**新しい名前は要求値を使う**。
- **中間状態の衝突を避ける順序で実行する**。001 の `autoResolve` が保証するのは**最終**名集合の一意性だけで、`a→b, b→c` のような入れ替えでは途中で既存ファイルとぶつかる。衝突しない順序に並べ替えて実行し、循環がある場合だけ一時名を1つ挟む。観測可能な要求(既存ファイルを上書きしない)は仕様側で固定する。
- **警告表示は 001 の `Warning` 4種をそのまま流す**(`DuplicateWarning` / `DigitShortageWarning` / `EmptyNameWarning` / `MissingSourceDateWarning`)。005 は判定を持たず、表示と実行可否の制御だけを担う。判定の正本は 001。
- **強制実行 = `autoResolve` の適用**。001 T6 が「強制実行時の名前確定」としてこの関数を用意済みなので、005 は独自の解決規則を作らない。
- 「元に戻す」は**元の URI へ戻すのではなく、新しい URI に対して元の名前で改名し直す**(ADR-001)。実行直前のスナップショット(ハンドル + 元の名前)をセッション内に1件だけ保持する。

旧ASDD 0.xの状態欄とログは`history/asdd-0.x-plan.md`へ凍結した。現在の状態・依存・Issue/PRは`plan.json`と各`task.json`を正本とする。

## 全体の受け入れ証拠

- `python <asdd-plugin>/scripts/workspace.py check specs`と仕様由来testがPASS
- `flutter test` / `flutter analyze` / `dart format --output=none --set-exit-if-changed .` が PASS
- 同一commit/buildのhost検証で、desktopの実rename・競合時不変・permission拒否・undoと、Android SAFの理由付きunsupported・file不変を確認する。
- 空ルールUIとdesktop更新日時ずらしを実装し、仕様由来test・manual確認・独立reviewを通す。
- `spec.md`の検証表を契約と一致させる。**2026-08-12に解消済み**(T09とT07のreviewで検出したdrift。VER-004へREQ-019/022、VER-005へREQ-019〜022を写した)。以後も契約側を正本とし、写しは契約の`verification`と全行一致していることを機械的に確認する。

## 人間の決定

| 日付 | 論点(要旨) | 決定 | 決定者 |
|------|------------|------|--------|
| 2026-08-01 | リネーム後の undo | セッション内・単一ステップ・時限トーストとして 005 に含める(永続履歴スタックは除外のまま) | 開発者 |
| 2026-08-02 | 警告表示の担当 | PRD §4.2 のリアルタイム警告は 005 が持つ。002 は持たない | 開発者 |
| 2026-08-03 | 更新日時ずらし | 任意・オプトインで 005 に含める。既定 OFF | 開発者 |
| 2026-08-05 | 基準日時不明の警告 | 無言で空文字になるのを止め、他の警告と同じ経路で表示する(005 の必須要求) | 開発者 |
| 2026-08-06 | 仕様レベル | **Strict**(実ファイルの破壊的操作・部分失敗・undo はデータ整合性の基準に該当) | 開発者承認 |
| 2026-08-06 | 部分失敗の扱い | **失敗した時点で停止し、成功済みの改名はそのまま残す**(巻き戻さない)。巻き戻し自体も失敗しうるため、実体とリストを一致させることを優先する。失敗した件と理由の提示は必須 | 開発者 |
| 2026-08-06 | 計画の承認 | 状態 draft → approved。レビュー判断ポイント(仕様レベル・タスクの切り方・スコープ)に個別異議なし、包括承認として記録 | 開発者 |
| 2026-08-07 | 巻き戻し期限の長さ | **5 秒**。値は契約の用語定義と REQ-007 に置き、偽の時計で検証する | 開発者 |
| 2026-08-07 | 停止時に残った一時名 | **その1件だけ元の名前へ戻すことを試みる**。一時名は成功した改名として記録されない内部ステップなので、「成功分は巻き戻さない」決定と衝突しない | 開発者 |
| 2026-08-07 | 仕様の承認 | 005 の契約を draft → **approved**。未解決事項2件への回答をもって承認 | 開発者承認 |
| 2026-08-07 | 空ルール時の扱い | ルールが空のときは実行不可・警告の代わりに「未設定」を提示(REQ-019/020)。空名と基準日時不明が重なるときは1件にまとめて提示(REQ-021)。空名になるファイルは強制実行でも除外(REQ-022) | 開発者 |
| 2026-08-07 | REQ-022 の強度 | 「強く止める」を**該当ファイルの除外**として具体化した案を承認。全体を実行不可にはしない | 開発者承認 |
| 2026-08-07 | 仕様の再承認 | T8 の更新を含む契約を draft → **approved** | 開発者承認 |
| 2026-08-12 | 巻き戻しと更新日時 | 更新日時ずらしを有効にして実行したあと巻き戻すと、名前は戻るが更新日時はずれたまま残る。**意図した受容**とする。契約の対象外へ明記し revision 3 とする | 開発者 |

## タスク

タスクのID・依存・状態は`plan.json`と各`tasks/*/task.json`が正本。番号は安定した識別子であり、実行順ではない。

| ID | 詳細 |
|---|---|
| T01 | [task.md](tasks/T01-define-behavior/task.md) |
| T02 | [task.md](tasks/T02-rename-orchestration/task.md) |
| T03 | [task.md](tasks/T03-preview-warnings/task.md) |
| T04 | [task.md](tasks/T04-confirmation-and-results/task.md) |
| T05 | [task.md](tasks/T05-platform-rename-adapters/task.md) |
| T06 | [task.md](tasks/T06-session-undo/task.md) |
| T07 | [task.md](tasks/T07-desktop-modified-time/task.md) |
| T08 | [task.md](tasks/T08-define-empty-rule-behavior/task.md) |
| T09 | [task.md](tasks/T09-empty-rule-ui/task.md) |
